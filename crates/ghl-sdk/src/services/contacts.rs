// @generated by xtask/generate_services.py — do not edit by hand.
//! `contacts` — typed methods for all 32 API v2 operations
//! in this module.
//!
//! These methods live on the same [`ContactsService`](crate::contacts::ContactsService) you get from [`Ghl::contacts`](crate::Ghl::contacts), alongside the hand-written helpers.
//!
//! Request and response types come from [`ghl_models::v2::contacts`](https://docs.rs/ghl-models/latest/ghl_models/v2/contacts/); every endpoint is also documented in the
//! [`contacts` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/contacts.md).
//!
//! Enable with `features = ["contacts"]`.

#![allow(clippy::too_many_arguments)]

use crate::error::Result;
use ghl_models::v2::contacts as models;

use crate::contacts::ContactsService;

/// Query parameters for [`ContactsService::get_contacts`].
#[derive(Debug, Clone, Default)]
pub struct GetContactsParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// Start After Id
    pub start_after_id: Option<String>,
    /// Start Afte
    pub start_after: Option<f64>,
    /// Contact Query
    pub query: Option<String>,
    /// Limit Per Page records count. will allow maximum up to 100 and default will be 20
    pub limit: Option<f64>,
}

impl GetContactsParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            ..Default::default()
        }
    }

    /// Start After Id
    pub fn start_after_id(mut self, v: impl Into<String>) -> Self {
        self.start_after_id = Some(v.into());
        self
    }

    /// Start Afte
    pub fn start_after(mut self, v: f64) -> Self {
        self.start_after = Some(v);
        self
    }

    /// Contact Query
    pub fn query(mut self, v: impl Into<String>) -> Self {
        self.query = Some(v.into());
        self
    }

    /// Limit Per Page records count. will allow maximum up to 100 and default will be 20
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        if let Some(v) = &self.start_after_id {
            q.push(("startAfterId".into(), v.to_string()));
        }
        if let Some(v) = &self.start_after {
            q.push(("startAfter".into(), v.to_string()));
        }
        if let Some(v) = &self.query {
            q.push(("query".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`ContactsService::get_contacts_by_business_id`].
#[derive(Debug, Clone, Default)]
pub struct GetContactsByBusinessIdParams {
    /// `limit` query parameter.
    pub limit: Option<String>,
    /// `locationId` query parameter.
    /// Required by the API.
    pub location_id: String,
    /// `skip` query parameter.
    pub skip: Option<String>,
    /// `query` query parameter.
    pub query: Option<String>,
}

impl GetContactsByBusinessIdParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            ..Default::default()
        }
    }

    /// Set the `limit` query parameter.
    pub fn limit(mut self, v: impl Into<String>) -> Self {
        self.limit = Some(v.into());
        self
    }

    /// Set the `skip` query parameter.
    pub fn skip(mut self, v: impl Into<String>) -> Self {
        self.skip = Some(v.into());
        self
    }

    /// Set the `query` query parameter.
    pub fn query(mut self, v: impl Into<String>) -> Self {
        self.query = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.skip {
            q.push(("skip".into(), v.to_string()));
        }
        if let Some(v) = &self.query {
            q.push(("query".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`ContactsService::get_duplicate_contact`].
#[derive(Debug, Clone, Default)]
pub struct GetDuplicateContactParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// Phone Number - Pass in URL Encoded form. i.e +1423164516 will become `%2B1423164516`
    pub number: Option<String>,
    /// Email - Pass in URL Encoded form. i.e test+abc@gmail.com will become
    /// `test%2Babc%40gmail.com`
    pub email: Option<String>,
}

impl GetDuplicateContactParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            ..Default::default()
        }
    }

    /// Phone Number - Pass in URL Encoded form. i.e +1423164516 will become `%2B1423164516`
    pub fn number(mut self, v: impl Into<String>) -> Self {
        self.number = Some(v.into());
        self
    }

    /// Email - Pass in URL Encoded form. i.e test+abc@gmail.com will become
    /// `test%2Babc%40gmail.com`
    pub fn email(mut self, v: impl Into<String>) -> Self {
        self.email = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        if let Some(v) = &self.number {
            q.push(("number".into(), v.to_string()));
        }
        if let Some(v) = &self.email {
            q.push(("email".into(), v.to_string()));
        }
        q
    }
}

impl ContactsService {
    /// Get Contacts
    ///
    /// Get Contacts **Note:** This API endpoint is deprecated. Please use the [Search
    /// Contacts](https://marketplace.gohighlevel.com/docs/ghl/contacts/search-contacts-advanced)
    /// endpoint instead.
    ///
    /// `GET /contacts/`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn get_contacts(
        &self,
        params: &GetContactsParams,
    ) -> Result<models::ContactsSearchSuccessfulResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/contacts/",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Create Contact
    ///
    /// Please find the list of acceptable values for the `country` field here
    ///
    /// `POST /contacts/`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn create_contact(
        &self,
        body: &models::CreateContactDto,
    ) -> Result<models::CreateContactsSuccessfulResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/contacts/",
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Add/Remove Contacts From Business
    ///
    /// Add/Remove Contacts From Business . Passing a `null` businessId will remove the
    /// businessId from the contacts
    ///
    /// `POST /contacts/bulk/business`
    pub async fn add_remove_contacts_from_business(
        &self,
        body: &models::ContactsBusinessUpdate,
    ) -> Result<models::ContactsBulkUpateResponse> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/contacts/bulk/business",
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Update Contacts Tags
    ///
    /// Allows you to update tags to multiple contacts at once, you can add or remove tags
    /// from the contacts
    ///
    /// `POST /contacts/bulk/tags/update/{type}`
    pub async fn update_contacts_tags(
        &self,
        type_: &str,
        body: &models::UpdateTagsDTO,
    ) -> Result<models::UpdateTagsResponseDTO> {
        let path = format!(
            "/contacts/bulk/tags/update/{}",
            crate::services::encode(type_)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Contacts By BusinessId
    ///
    /// `GET /contacts/business/{businessId}`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn get_contacts_by_business_id(
        &self,
        business_id: &str,
        params: &GetContactsByBusinessIdParams,
    ) -> Result<models::ContactsSearchSuccessfulResponseDto> {
        let path = format!(
            "/contacts/business/{}",
            crate::services::encode(business_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Search Contacts
    ///
    /// Search contacts based on combinations of advanced filters. Documentation Link -
    /// <https://doc.clickup.com/8631005/d/h/87cpx-158396/6e629989abe7fad>
    ///
    /// `POST /contacts/search`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn search_contacts(
        &self,
        body: &models::SearchBodyV2DTO,
    ) -> Result<serde_json::Value> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/contacts/search",
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Duplicate Contact
    ///
    /// Get Duplicate Contact. If `Allow Duplicate Contact` is disabled under Settings, the
    /// global unique identifier will be used for searching the contact. If the setting is
    /// enabled, first priority for search is `email` and the second priority will be
    /// `phone`.
    ///
    /// `GET /contacts/search/duplicate`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn get_duplicate_contact(
        &self,
        params: &GetDuplicateContactParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/contacts/search/duplicate",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Upsert Contact
    ///
    /// Please find the list of acceptable values for the `country` field here The Upsert
    /// API will adhere to the configuration defined under the “Allow Duplicate Contact”
    /// setting at the Location level. If the setting is configured to check both Email and
    /// Phone, the API will attempt to identify an existing contact based on the priority
    /// sequence specified in the setting, and will create or update the contact
    /// accordingly. If two separate contacts already exist—one with the same email and
    /// another w
    ///
    /// `POST /contacts/upsert`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn upsert_contact(
        &self,
        body: &models::UpsertContactDto,
    ) -> Result<models::UpsertContactsSuccessfulResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/contacts/upsert",
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Delete Contact
    ///
    /// `DELETE /contacts/{contactId}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn delete_contact(
        &self,
        contact_id: &str,
    ) -> Result<models::DeleteContactsSuccessfulResponseDto> {
        let path = format!("/contacts/{}", crate::services::encode(contact_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Contact
    ///
    /// `GET /contacts/{contactId}`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn get_contact(
        &self,
        contact_id: &str,
    ) -> Result<models::ContactsByIdSuccessfulResponseDto> {
        let path = format!("/contacts/{}", crate::services::encode(contact_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Update Contact
    ///
    /// Please find the list of acceptable values for the `country` field here
    ///
    /// `PUT /contacts/{contactId}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn update_contact(
        &self,
        contact_id: &str,
        body: &models::UpdateContactDto,
    ) -> Result<models::UpdateContactsSuccessfulResponseDto> {
        let path = format!("/contacts/{}", crate::services::encode(contact_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Appointments for Contact
    ///
    /// `GET /contacts/{contactId}/appointments`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn get_appointments_for_contact(
        &self,
        contact_id: &str,
    ) -> Result<models::GetEventsSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/appointments",
            crate::services::encode(contact_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Remove Contact From Every Campaign
    ///
    /// `DELETE /contacts/{contactId}/campaigns/removeAll`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn remove_contact_from_every_campaign(
        &self,
        contact_id: &str,
    ) -> Result<models::CreateDeleteCantactsCampaignsSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/campaigns/removeAll",
            crate::services::encode(contact_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Remove Contact From Campaign
    ///
    /// `DELETE /contacts/{contactId}/campaigns/{campaignId}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn remove_contact_from_campaign(
        &self,
        contact_id: &str,
        campaign_id: &str,
    ) -> Result<models::CreateDeleteCantactsCampaignsSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/campaigns/{}",
            crate::services::encode(contact_id),
            crate::services::encode(campaign_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Add Contact to Campaign
    ///
    /// Add contact to Campaign
    ///
    /// `POST /contacts/{contactId}/campaigns/{campaignId}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn add_contact_to_campaign(
        &self,
        contact_id: &str,
        campaign_id: &str,
        body: &models::AddContactToCampaignDto,
    ) -> Result<models::CreateDeleteCantactsCampaignsSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/campaigns/{}",
            crate::services::encode(contact_id),
            crate::services::encode(campaign_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Remove Followers
    ///
    /// `DELETE /contacts/{contactId}/followers`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn remove_followers(
        &self,
        contact_id: &str,
        body: &models::FollowersDTO,
    ) -> Result<models::DeleteFollowersSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/followers",
            crate::services::encode(contact_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Add Followers
    ///
    /// `POST /contacts/{contactId}/followers`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn add_followers(
        &self,
        contact_id: &str,
        body: &models::FollowersDTO,
    ) -> Result<models::CreateAddFollowersSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/followers",
            crate::services::encode(contact_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get All Notes
    ///
    /// `GET /contacts/{contactId}/notes`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn get_all_notes(
        &self,
        contact_id: &str,
    ) -> Result<models::GetNotesListSuccessfulResponseDto> {
        let path = format!("/contacts/{}/notes", crate::services::encode(contact_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Create Note
    ///
    /// `POST /contacts/{contactId}/notes`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn create_note(
        &self,
        contact_id: &str,
        body: &models::NotesDTO,
    ) -> Result<models::GetCreateUpdateNoteSuccessfulResponseDto> {
        let path = format!("/contacts/{}/notes", crate::services::encode(contact_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Delete Note
    ///
    /// `DELETE /contacts/{contactId}/notes/{id}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn delete_note(
        &self,
        contact_id: &str,
        id: &str,
    ) -> Result<models::DeleteNoteSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/notes/{}",
            crate::services::encode(contact_id),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Note
    ///
    /// `GET /contacts/{contactId}/notes/{id}`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn get_note(
        &self,
        contact_id: &str,
        id: &str,
    ) -> Result<models::GetCreateUpdateNoteSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/notes/{}",
            crate::services::encode(contact_id),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Update Note
    ///
    /// `PUT /contacts/{contactId}/notes/{id}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn update_note(
        &self,
        contact_id: &str,
        id: &str,
        body: &models::UpdateNoteDTO,
    ) -> Result<models::GetCreateUpdateNoteSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/notes/{}",
            crate::services::encode(contact_id),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Remove Tags
    ///
    /// `DELETE /contacts/{contactId}/tags`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn remove_tags(
        &self,
        contact_id: &str,
        body: &models::TagsDTO,
    ) -> Result<models::CreateDeleteTagSuccessfulResponseDto> {
        let path = format!("/contacts/{}/tags", crate::services::encode(contact_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Add Tags
    ///
    /// `POST /contacts/{contactId}/tags`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn add_tags(
        &self,
        contact_id: &str,
        body: &models::TagsDTO,
    ) -> Result<models::CreateAddTagSuccessfulResponseDto> {
        let path = format!("/contacts/{}/tags", crate::services::encode(contact_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get all Tasks
    ///
    /// `GET /contacts/{contactId}/tasks`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn get_all_tasks(
        &self,
        contact_id: &str,
    ) -> Result<models::TasksListSuccessfulResponseDto> {
        let path = format!("/contacts/{}/tasks", crate::services::encode(contact_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Create Task
    ///
    /// `POST /contacts/{contactId}/tasks`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn create_task(
        &self,
        contact_id: &str,
        body: &models::CreateTaskParams,
    ) -> Result<models::TaskByIsSuccessfulResponseDto> {
        let path = format!("/contacts/{}/tasks", crate::services::encode(contact_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Delete Task
    ///
    /// `DELETE /contacts/{contactId}/tasks/{taskId}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn delete_task(
        &self,
        contact_id: &str,
        task_id: &str,
    ) -> Result<models::DeleteTaskSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/tasks/{}",
            crate::services::encode(contact_id),
            crate::services::encode(task_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Task
    ///
    /// `GET /contacts/{contactId}/tasks/{taskId}`
    ///
    /// Requires scope: `contacts.readonly`.
    pub async fn get_task(
        &self,
        contact_id: &str,
        task_id: &str,
    ) -> Result<models::TaskByIsSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/tasks/{}",
            crate::services::encode(contact_id),
            crate::services::encode(task_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Update Task
    ///
    /// `PUT /contacts/{contactId}/tasks/{taskId}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn update_task(
        &self,
        contact_id: &str,
        task_id: &str,
        body: &models::UpdateTaskBody,
    ) -> Result<models::TaskByIsSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/tasks/{}",
            crate::services::encode(contact_id),
            crate::services::encode(task_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Update Task Completed
    ///
    /// `PUT /contacts/{contactId}/tasks/{taskId}/completed`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn update_task_completed(
        &self,
        contact_id: &str,
        task_id: &str,
        body: &models::UpdateTaskStatusParams,
    ) -> Result<models::TaskByIsSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/tasks/{}/completed",
            crate::services::encode(contact_id),
            crate::services::encode(task_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Delete Contact from Workflow
    ///
    /// `DELETE /contacts/{contactId}/workflow/{workflowId}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn delete_contact_from_workflow(
        &self,
        contact_id: &str,
        workflow_id: &str,
        body: &models::CreateWorkflowDto,
    ) -> Result<models::ContactsWorkflowSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/workflow/{}",
            crate::services::encode(contact_id),
            crate::services::encode(workflow_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Add Contact to Workflow
    ///
    /// `POST /contacts/{contactId}/workflow/{workflowId}`
    ///
    /// Requires scope: `contacts.write`.
    pub async fn add_contact_to_workflow(
        &self,
        contact_id: &str,
        workflow_id: &str,
        body: &models::CreateWorkflowDto,
    ) -> Result<models::ContactsWorkflowSuccessfulResponseDto> {
        let path = format!(
            "/contacts/{}/workflow/{}",
            crate::services::encode(contact_id),
            crate::services::encode(workflow_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }
}
