// @generated by xtask/generate_services.py — do not edit by hand.
//! `payments` — typed methods for all 23 API v3 operations
//! in this module.
//!
//! Access via [`Ghl::v3`](crate::Ghl::v3)`().payments()`. These endpoints send `Version: v3`.
//!
//! Request and response types come from [`ghl_models::v3::payments`](https://docs.rs/ghl-models/latest/ghl_models/v3/payments/); every endpoint is also documented in the
//! [`payments` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/payments.md).
//!
//! Enable with `features = ["payments"]`.

#![allow(clippy::too_many_arguments)]

use crate::client::Ghl;
use crate::error::Result;
use ghl_models::v3::payments as models;

/// Typed access to the `payments` API v3 surface (23 operations). Obtained via
/// [`Ghl::v3`](crate::Ghl::v3)`().payments()`.
#[derive(Debug, Clone)]
pub struct PaymentsService {
    pub(crate) client: Ghl,
}

impl PaymentsService {
    pub(crate) fn new(client: Ghl) -> Self {
        Self { client }
    }
}

/// Query parameters for [`PaymentsService::fetch_coupon`].
#[derive(Debug, Clone, Default)]
pub struct FetchCouponParams {
    /// Location Id
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// Coupon id
    /// Required by the API.
    pub id: String,
    /// Coupon code
    /// Required by the API.
    pub code: String,
}

impl FetchCouponParams {
    /// Start from the parameters the API requires.
    pub fn new(
        alt_id: impl Into<String>,
        alt_type: impl Into<String>,
        id: impl Into<String>,
        code: impl Into<String>,
    ) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            id: id.into(),
            code: code.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
            ("id".into(), self.id.clone()),
            ("code".into(), self.code.clone()),
        ];
        q
    }
}

/// Query parameters for [`PaymentsService::list_coupons`].
#[derive(Debug, Clone, Default)]
pub struct ListCouponsParams {
    /// Location Id
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// Maximum number of coupons to return
    pub limit: Option<f64>,
    /// Number of coupons to skip for pagination
    pub offset: Option<f64>,
    /// Filter coupons by status
    /// Allowed values: `scheduled`, `active`, `expired`.
    pub status: Option<String>,
    /// Search term to filter coupons by name or code
    pub search: Option<String>,
}

impl ListCouponsParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            ..Default::default()
        }
    }

    /// Maximum number of coupons to return
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// Number of coupons to skip for pagination
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    /// Filter coupons by status
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    /// Search term to filter coupons by name or code
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`PaymentsService::fetch_given_provider_config`].
#[derive(Debug, Clone, Default)]
pub struct FetchGivenProviderConfigParams {
    /// Location id
    /// Required by the API.
    pub location_id: String,
}

impl FetchGivenProviderConfigParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        q
    }
}

/// Query parameters for [`PaymentsService::create_new_provider_config`].
#[derive(Debug, Clone, Default)]
pub struct CreateNewProviderConfigParams {
    /// Location id
    /// Required by the API.
    pub location_id: String,
}

impl CreateNewProviderConfigParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        q
    }
}

/// Query parameters for [`PaymentsService::disconnect_existing_provider_config`].
#[derive(Debug, Clone, Default)]
pub struct DisconnectExistingProviderConfigParams {
    /// Location id
    /// Required by the API.
    pub location_id: String,
}

impl DisconnectExistingProviderConfigParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        q
    }
}

/// Query parameters for [`PaymentsService::deleting_an_existing_integration`].
#[derive(Debug, Clone, Default)]
pub struct DeletingAnExistingIntegrationParams {
    /// Location id
    /// Required by the API.
    pub location_id: String,
}

impl DeletingAnExistingIntegrationParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        q
    }
}

/// Query parameters for [`PaymentsService::create_new_integration`].
#[derive(Debug, Clone, Default)]
pub struct CreateNewIntegrationParams {
    /// Location id
    /// Required by the API.
    pub location_id: String,
}

impl CreateNewIntegrationParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        q
    }
}

/// Query parameters for [`PaymentsService::list_white_label_integration_providers`].
#[derive(Debug, Clone, Default)]
pub struct ListWhiteLabelIntegrationProvidersParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// The maximum number of items to be included in a single page of results
    pub limit: Option<f64>,
    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub offset: Option<f64>,
}

impl ListWhiteLabelIntegrationProvidersParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            ..Default::default()
        }
    }

    /// The maximum number of items to be included in a single page of results
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`PaymentsService::list_orders`].
#[derive(Debug, Clone, Default)]
pub struct ListOrdersParams {
    /// LocationId is the id of the sub-account.
    pub location_id: Option<String>,
    /// AltId is the unique identifier e.g: location id.
    /// Required by the API.
    pub alt_id: String,
    /// Order status.
    pub status: Option<String>,
    /// Payment Status of the Order
    /// Allowed values: `paid`, `unpaid`, `refunded`, `partially_paid`.
    pub payment_status: Option<String>,
    /// Mode of payment.
    pub payment_mode: Option<String>,
    /// Starting interval of orders.
    pub start_at: Option<String>,
    /// Closing interval of orders.
    pub end_at: Option<String>,
    /// The name of the order for searching.
    pub search: Option<String>,
    /// Contact id for filtering of orders.
    pub contact_id: Option<String>,
    /// Funnel product ids separated by comma.
    pub funnel_product_ids: Option<String>,
    /// Source id
    pub source_id: Option<String>,
    /// The maximum number of items to be included in a single page of results
    pub limit: Option<f64>,
    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub offset: Option<f64>,
}

impl ListOrdersParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            ..Default::default()
        }
    }

    /// LocationId is the id of the sub-account.
    pub fn location_id(mut self, v: impl Into<String>) -> Self {
        self.location_id = Some(v.into());
        self
    }

    /// Order status.
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    /// Payment Status of the Order
    pub fn payment_status(mut self, v: impl Into<String>) -> Self {
        self.payment_status = Some(v.into());
        self
    }

    /// Mode of payment.
    pub fn payment_mode(mut self, v: impl Into<String>) -> Self {
        self.payment_mode = Some(v.into());
        self
    }

    /// Starting interval of orders.
    pub fn start_at(mut self, v: impl Into<String>) -> Self {
        self.start_at = Some(v.into());
        self
    }

    /// Closing interval of orders.
    pub fn end_at(mut self, v: impl Into<String>) -> Self {
        self.end_at = Some(v.into());
        self
    }

    /// The name of the order for searching.
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// Contact id for filtering of orders.
    pub fn contact_id(mut self, v: impl Into<String>) -> Self {
        self.contact_id = Some(v.into());
        self
    }

    /// Funnel product ids separated by comma.
    pub fn funnel_product_ids(mut self, v: impl Into<String>) -> Self {
        self.funnel_product_ids = Some(v.into());
        self
    }

    /// Source id
    pub fn source_id(mut self, v: impl Into<String>) -> Self {
        self.source_id = Some(v.into());
        self
    }

    /// The maximum number of items to be included in a single page of results
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("altId".into(), self.alt_id.clone())];
        if let Some(v) = &self.location_id {
            q.push(("locationId".into(), v.to_string()));
        }
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        if let Some(v) = &self.payment_status {
            q.push(("paymentStatus".into(), v.to_string()));
        }
        if let Some(v) = &self.payment_mode {
            q.push(("paymentMode".into(), v.to_string()));
        }
        if let Some(v) = &self.start_at {
            q.push(("startAt".into(), v.to_string()));
        }
        if let Some(v) = &self.end_at {
            q.push(("endAt".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.contact_id {
            q.push(("contactId".into(), v.to_string()));
        }
        if let Some(v) = &self.funnel_product_ids {
            q.push(("funnelProductIds".into(), v.to_string()));
        }
        if let Some(v) = &self.source_id {
            q.push(("sourceId".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`PaymentsService::get_order_by_id`].
#[derive(Debug, Clone, Default)]
pub struct GetOrderByIdParams {
    /// LocationId is the id of the sub-account.
    pub location_id: Option<String>,
    /// AltId is the unique identifier e.g: location id.
    /// Required by the API.
    pub alt_id: String,
}

impl GetOrderByIdParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            ..Default::default()
        }
    }

    /// LocationId is the id of the sub-account.
    pub fn location_id(mut self, v: impl Into<String>) -> Self {
        self.location_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("altId".into(), self.alt_id.clone())];
        if let Some(v) = &self.location_id {
            q.push(("locationId".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`PaymentsService::list_fulfillment`].
#[derive(Debug, Clone, Default)]
pub struct ListFulfillmentParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl ListFulfillmentParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`PaymentsService::list_order_notes`].
#[derive(Debug, Clone, Default)]
pub struct ListOrderNotesParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl ListOrderNotesParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`PaymentsService::list_subscriptions`].
#[derive(Debug, Clone, Default)]
pub struct ListSubscriptionsParams {
    /// AltId is the unique identifier e.g: location id.
    /// Required by the API.
    pub alt_id: String,
    /// AltType is the type of identifier.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// Entity id for filtering of subscriptions.
    pub entity_id: Option<String>,
    /// Mode of payment.
    pub payment_mode: Option<String>,
    /// Starting interval of subscriptions.
    pub start_at: Option<String>,
    /// Closing interval of subscriptions.
    pub end_at: Option<String>,
    /// Source of the subscriptions.
    pub entity_source_type: Option<String>,
    /// The name of the subscription for searching.
    pub search: Option<String>,
    /// Contact ID for the subscription
    pub contact_id: Option<String>,
    /// Subscription id for filtering of subscriptions.
    pub id: Option<String>,
    /// The maximum number of items to be included in a single page of results
    pub limit: Option<f64>,
    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub offset: Option<f64>,
    /// Get the total payments collected for the subscription.
    pub get_payments_collected_count: Option<bool>,
}

impl ListSubscriptionsParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            ..Default::default()
        }
    }

    /// Entity id for filtering of subscriptions.
    pub fn entity_id(mut self, v: impl Into<String>) -> Self {
        self.entity_id = Some(v.into());
        self
    }

    /// Mode of payment.
    pub fn payment_mode(mut self, v: impl Into<String>) -> Self {
        self.payment_mode = Some(v.into());
        self
    }

    /// Starting interval of subscriptions.
    pub fn start_at(mut self, v: impl Into<String>) -> Self {
        self.start_at = Some(v.into());
        self
    }

    /// Closing interval of subscriptions.
    pub fn end_at(mut self, v: impl Into<String>) -> Self {
        self.end_at = Some(v.into());
        self
    }

    /// Source of the subscriptions.
    pub fn entity_source_type(mut self, v: impl Into<String>) -> Self {
        self.entity_source_type = Some(v.into());
        self
    }

    /// The name of the subscription for searching.
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// Contact ID for the subscription
    pub fn contact_id(mut self, v: impl Into<String>) -> Self {
        self.contact_id = Some(v.into());
        self
    }

    /// Subscription id for filtering of subscriptions.
    pub fn id(mut self, v: impl Into<String>) -> Self {
        self.id = Some(v.into());
        self
    }

    /// The maximum number of items to be included in a single page of results
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    /// Get the total payments collected for the subscription.
    pub fn get_payments_collected_count(mut self, v: bool) -> Self {
        self.get_payments_collected_count = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        if let Some(v) = &self.entity_id {
            q.push(("entityId".into(), v.to_string()));
        }
        if let Some(v) = &self.payment_mode {
            q.push(("paymentMode".into(), v.to_string()));
        }
        if let Some(v) = &self.start_at {
            q.push(("startAt".into(), v.to_string()));
        }
        if let Some(v) = &self.end_at {
            q.push(("endAt".into(), v.to_string()));
        }
        if let Some(v) = &self.entity_source_type {
            q.push(("entitySourceType".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.contact_id {
            q.push(("contactId".into(), v.to_string()));
        }
        if let Some(v) = &self.id {
            q.push(("id".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        if let Some(v) = &self.get_payments_collected_count {
            q.push(("getPaymentsCollectedCount".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`PaymentsService::get_subscription_by_id`].
#[derive(Debug, Clone, Default)]
pub struct GetSubscriptionByIdParams {
    /// AltId is the unique identifier e.g: location id.
    /// Required by the API.
    pub alt_id: String,
    /// AltType is the type of identifier.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GetSubscriptionByIdParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`PaymentsService::list_transactions`].
#[derive(Debug, Clone, Default)]
pub struct ListTransactionsParams {
    /// LocationId is the id of the sub-account.
    pub location_id: Option<String>,
    /// AltId is the unique identifier e.g: location id.
    /// Required by the API.
    pub alt_id: String,
    /// AltType is the type of identifier.
    /// Required by the API.
    pub alt_type: String,
    /// Mode of payment.
    pub payment_mode: Option<String>,
    /// Starting interval of transactions.
    pub start_at: Option<String>,
    /// Closing interval of transactions.
    pub end_at: Option<String>,
    /// Source of the transactions.
    pub entity_source_type: Option<String>,
    /// Source sub-type of the transactions.
    pub entity_source_sub_type: Option<String>,
    /// The name of the transaction for searching.
    pub search: Option<String>,
    /// Subscription id for filtering of transactions.
    pub subscription_id: Option<String>,
    /// Entity id for filtering of transactions.
    pub entity_id: Option<String>,
    /// Contact id for filtering of transactions.
    pub contact_id: Option<String>,
    /// The maximum number of items to be included in a single page of results
    pub limit: Option<f64>,
    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub offset: Option<f64>,
}

impl ListTransactionsParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            ..Default::default()
        }
    }

    /// LocationId is the id of the sub-account.
    pub fn location_id(mut self, v: impl Into<String>) -> Self {
        self.location_id = Some(v.into());
        self
    }

    /// Mode of payment.
    pub fn payment_mode(mut self, v: impl Into<String>) -> Self {
        self.payment_mode = Some(v.into());
        self
    }

    /// Starting interval of transactions.
    pub fn start_at(mut self, v: impl Into<String>) -> Self {
        self.start_at = Some(v.into());
        self
    }

    /// Closing interval of transactions.
    pub fn end_at(mut self, v: impl Into<String>) -> Self {
        self.end_at = Some(v.into());
        self
    }

    /// Source of the transactions.
    pub fn entity_source_type(mut self, v: impl Into<String>) -> Self {
        self.entity_source_type = Some(v.into());
        self
    }

    /// Source sub-type of the transactions.
    pub fn entity_source_sub_type(mut self, v: impl Into<String>) -> Self {
        self.entity_source_sub_type = Some(v.into());
        self
    }

    /// The name of the transaction for searching.
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// Subscription id for filtering of transactions.
    pub fn subscription_id(mut self, v: impl Into<String>) -> Self {
        self.subscription_id = Some(v.into());
        self
    }

    /// Entity id for filtering of transactions.
    pub fn entity_id(mut self, v: impl Into<String>) -> Self {
        self.entity_id = Some(v.into());
        self
    }

    /// Contact id for filtering of transactions.
    pub fn contact_id(mut self, v: impl Into<String>) -> Self {
        self.contact_id = Some(v.into());
        self
    }

    /// The maximum number of items to be included in a single page of results
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        if let Some(v) = &self.location_id {
            q.push(("locationId".into(), v.to_string()));
        }
        if let Some(v) = &self.payment_mode {
            q.push(("paymentMode".into(), v.to_string()));
        }
        if let Some(v) = &self.start_at {
            q.push(("startAt".into(), v.to_string()));
        }
        if let Some(v) = &self.end_at {
            q.push(("endAt".into(), v.to_string()));
        }
        if let Some(v) = &self.entity_source_type {
            q.push(("entitySourceType".into(), v.to_string()));
        }
        if let Some(v) = &self.entity_source_sub_type {
            q.push(("entitySourceSubType".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.subscription_id {
            q.push(("subscriptionId".into(), v.to_string()));
        }
        if let Some(v) = &self.entity_id {
            q.push(("entityId".into(), v.to_string()));
        }
        if let Some(v) = &self.contact_id {
            q.push(("contactId".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`PaymentsService::get_transaction_by_id`].
#[derive(Debug, Clone, Default)]
pub struct GetTransactionByIdParams {
    /// LocationId is the id of the sub-account.
    pub location_id: Option<String>,
    /// AltId is the unique identifier e.g: location id.
    /// Required by the API.
    pub alt_id: String,
    /// AltType is the type of identifier.
    /// Required by the API.
    pub alt_type: String,
}

impl GetTransactionByIdParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            ..Default::default()
        }
    }

    /// LocationId is the id of the sub-account.
    pub fn location_id(mut self, v: impl Into<String>) -> Self {
        self.location_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        if let Some(v) = &self.location_id {
            q.push(("locationId".into(), v.to_string()));
        }
        q
    }
}

impl PaymentsService {
    /// Delete Coupon
    ///
    /// The "Delete Coupon" API allows you to permanently remove a coupon from your system
    /// using its unique identifier. Use this endpoint to discontinue promotional offers or
    /// clean up unused coupons. Note that this action cannot be undone.
    ///
    /// `DELETE /payments/coupon`
    ///
    /// Requires scope: `payments/coupons.write`.
    pub async fn delete_coupon(
        &self,
        body: &models::DeleteCouponParams,
    ) -> Result<models::DeleteCouponResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                "/payments/coupon",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Fetch Coupon
    ///
    /// The "Get Coupon Details" API enables you to retrieve comprehensive information about
    /// a specific coupon using either its unique identifier or promotional code. Use this
    /// endpoint to view coupon parameters, usage statistics, validity periods, and other
    /// promotional details.
    ///
    /// `GET /payments/coupon`
    ///
    /// Requires scope: `payments/coupons.readonly`.
    pub async fn fetch_coupon(
        &self,
        params: &FetchCouponParams,
    ) -> Result<models::CreateCouponResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/payments/coupon",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create Coupon
    ///
    /// The "Create Coupon" API allows you to create a new promotional coupon with
    /// customizable parameters such as discount amount, validity period, usage limits, and
    /// applicable products. Use this endpoint to set up promotional offers and special
    /// discounts for your customers.
    ///
    /// `POST /payments/coupon`
    ///
    /// Requires scope: `payments/coupons.write`.
    pub async fn create_coupon(
        &self,
        body: &models::CreateCouponParams,
    ) -> Result<models::CreateCouponResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/payments/coupon",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Update Coupon
    ///
    /// The "Update Coupon" API enables you to modify existing coupon details such as
    /// discount values, validity periods, usage limits, and other promotional parameters.
    /// Use this endpoint to adjust or extend promotional offers for your customers.
    ///
    /// `PUT /payments/coupon`
    ///
    /// Requires scope: `payments/coupons.write`.
    pub async fn update_coupon(
        &self,
        body: &models::UpdateCouponParams,
    ) -> Result<models::CreateCouponResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                "/payments/coupon",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// List Coupons
    ///
    /// The "List Coupons" API allows you to retrieve a list of all coupons available in
    /// your location. Use this endpoint to view all promotional offers and special
    /// discounts for your customers.
    ///
    /// `GET /payments/coupon/list`
    ///
    /// Requires scope: `payments/coupons.readonly`.
    pub async fn list_coupons(
        &self,
        params: &ListCouponsParams,
    ) -> Result<models::ListCouponsResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/payments/coupon/list",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Custom-provider marketplace app update capabilities
    ///
    /// Toggle capabilities for the marketplace app tied to the OAuth client
    ///
    /// `PUT /payments/custom-provider/capabilities`
    ///
    /// Requires scope: `payments/custom-provider.write`.
    pub async fn custom_provider_marketplace_app_update_capabilities(
        &self,
        body: &models::UpdateCustomProviderCapabilitiesDto,
    ) -> Result<models::UpdateCustomProviderCapabilitiesResponseSchema> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                "/payments/custom-provider/capabilities",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Fetch given provider config
    ///
    /// API for fetching an existing payment config for given location
    ///
    /// `GET /payments/custom-provider/connect`
    ///
    /// Requires scope: `payments/custom-provider.readonly`.
    pub async fn fetch_given_provider_config(
        &self,
        params: &FetchGivenProviderConfigParams,
    ) -> Result<models::GetCustomProvidersResponseSchema> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/payments/custom-provider/connect",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create new provider config
    ///
    /// API to create a new payment config for given location
    ///
    /// `POST /payments/custom-provider/connect`
    ///
    /// Requires scope: `payments/custom-provider.write`.
    pub async fn create_new_provider_config(
        &self,
        params: &CreateNewProviderConfigParams,
        body: &models::ConnectCustomProvidersConfigDto,
    ) -> Result<models::ConnectCustomProvidersResponseSchema> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/payments/custom-provider/connect",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Disconnect existing provider config
    ///
    /// API to disconnect an existing payment config for given location
    ///
    /// `POST /payments/custom-provider/disconnect`
    ///
    /// Requires scope: `payments/custom-provider.write`.
    pub async fn disconnect_existing_provider_config(
        &self,
        params: &DisconnectExistingProviderConfigParams,
        body: &models::DeleteCustomProvidersConfigDto,
    ) -> Result<models::DisconnectCustomProvidersResponseSchema> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/payments/custom-provider/disconnect",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Deleting an existing integration
    ///
    /// API to delete an association for an app and location
    ///
    /// `DELETE /payments/custom-provider/provider`
    ///
    /// Requires scope: `payments/custom-provider.write`.
    pub async fn deleting_an_existing_integration(
        &self,
        params: &DeletingAnExistingIntegrationParams,
    ) -> Result<models::DeleteCustomProvidersResponseSchema> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                "/payments/custom-provider/provider",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create new integration
    ///
    /// API to create a new association for an app and location
    ///
    /// `POST /payments/custom-provider/provider`
    ///
    /// Requires scope: `payments/custom-provider.write`.
    pub async fn create_new_integration(
        &self,
        params: &CreateNewIntegrationParams,
        body: &models::CreateCustomProvidersDto,
    ) -> Result<models::CreateCustomProvidersResponseSchema> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/payments/custom-provider/provider",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// List White-label Integration Providers
    ///
    /// The "List White-label Integration Providers" API allows to retrieve a paginated list
    /// of integration providers. Customize your results by filtering whitelabel integration
    /// providers(which are built directly on top of Authorize.net or NMI) based on name or
    /// paginate through the list using the provided query parameters. This endpoint
    /// provides a straightforward way to explore and retrieve integration provider
    /// information.
    ///
    /// `GET /payments/integrations/provider/whitelabel`
    ///
    /// Requires scope: `payments/integration.readonly`.
    pub async fn list_white_label_integration_providers(
        &self,
        params: &ListWhiteLabelIntegrationProvidersParams,
    ) -> Result<models::ListWhitelabelIntegrationProviderResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/payments/integrations/provider/whitelabel",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create White-label Integration Provider
    ///
    /// The "Create White-label Integration Provider" API allows adding a new payment
    /// provider integration to the system which is built on top of Authorize.net or NMI.
    /// Use this endpoint to create a integration provider with the specified details.
    /// Ensure that the required information is provided in the request payload. This
    /// endpoint can be only invoked using marketplace-app token
    ///
    /// `POST /payments/integrations/provider/whitelabel`
    ///
    /// Requires scope: `payments/integration.write`.
    pub async fn create_white_label_integration_provider(
        &self,
        body: &models::CreateWhiteLabelIntegrationProviderDto,
    ) -> Result<models::CreateWhitelabelIntegrationResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/payments/integrations/provider/whitelabel",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// List Orders
    ///
    /// The "List Orders" API allows to retrieve a paginated list of orders. Customize your
    /// results by filtering orders based on name, alt type, order status, payment mode,
    /// date range, type of source, contact, funnel products or paginate through the list
    /// using the provided query parameters. This endpoint provides a straightforward way to
    /// explore and retrieve order information.
    ///
    /// `GET /payments/orders`
    ///
    /// Requires scope: `payments/orders.readonly`.
    pub async fn list_orders(
        &self,
        params: &ListOrdersParams,
    ) -> Result<models::ListOrdersResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/payments/orders",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get Order by ID
    ///
    /// The "Get Order by ID" API allows to retrieve information for a specific order using
    /// its unique identifier. Use this endpoint to fetch details for a single order based
    /// on the provided order ID.
    ///
    /// `GET /payments/orders/{orderId}`
    ///
    /// Requires scope: `payments/orders.readonly`.
    pub async fn get_order_by_id(
        &self,
        order_id: &str,
        params: &GetOrderByIdParams,
    ) -> Result<models::GetOrderResponseSchema> {
        let path = format!("/payments/orders/{}", crate::services::encode(order_id));
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// List fulfillment
    ///
    /// List all fulfillment history of an order
    ///
    /// `GET /payments/orders/{orderId}/fulfillments`
    ///
    /// Requires scope: `payments/orders.readonly`.
    pub async fn list_fulfillment(
        &self,
        order_id: &str,
        params: &ListFulfillmentParams,
    ) -> Result<models::ListFulfillmentResponseDto> {
        let path = format!(
            "/payments/orders/{}/fulfillments",
            crate::services::encode(order_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Create order fulfillment
    ///
    /// The "Order Fulfillment" API facilitates the process of fulfilling an order.
    ///
    /// `POST /payments/orders/{orderId}/fulfillments`
    ///
    /// Requires scope: `payments/orders.write`.
    pub async fn create_order_fulfillment(
        &self,
        order_id: &str,
        body: &models::CreateFulfillmentDto,
    ) -> Result<models::CreateFulfillmentResponseDto> {
        let path = format!(
            "/payments/orders/{}/fulfillments",
            crate::services::encode(order_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// List Order Notes
    ///
    /// List all notes of an order
    ///
    /// `GET /payments/orders/{orderId}/notes`
    pub async fn list_order_notes(
        &self,
        order_id: &str,
        params: &ListOrderNotesParams,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/payments/orders/{}/notes",
            crate::services::encode(order_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Record Order Payment
    ///
    /// The "Record Order Payment" API allows to record a payment for an order. Use this
    /// endpoint to record payment for an order and update the order status to "Paid".
    ///
    /// `POST /payments/orders/{orderId}/record-payment`
    ///
    /// Requires scope: `payments/orders.collectPayment`.
    pub async fn record_order_payment(
        &self,
        order_id: &str,
        body: &models::PostRecordOrderPaymentBody,
    ) -> Result<models::PostRecordOrderPaymentResponse> {
        let path = format!(
            "/payments/orders/{}/record-payment",
            crate::services::encode(order_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// List Subscriptions
    ///
    /// The "List Subscriptions" API allows to retrieve a paginated list of subscriptions.
    /// Customize your results by filtering subscriptions based on name, alt type,
    /// subscription status, payment mode, date range, type of source, contact, subscription
    /// id, entity id, contact or paginate through the list using the provided query
    /// parameters. This endpoint provides a straightforward way to explore and retrieve
    /// subscription information.
    ///
    /// `GET /payments/subscriptions`
    ///
    /// Requires scope: `payments/subscriptions.readonly`.
    pub async fn list_subscriptions(
        &self,
        params: &ListSubscriptionsParams,
    ) -> Result<models::ListSubscriptionResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/payments/subscriptions",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get Subscription by ID
    ///
    /// The "Get Subscription by ID" API allows to retrieve information for a specific
    /// subscription using its unique identifier. Use this endpoint to fetch details for a
    /// single subscription based on the provided subscription ID.
    ///
    /// `GET /payments/subscriptions/{subscriptionId}`
    ///
    /// Requires scope: `payments/subscriptions.readonly`.
    pub async fn get_subscription_by_id(
        &self,
        subscription_id: &str,
        params: &GetSubscriptionByIdParams,
    ) -> Result<models::GetSubscriptionResponseSchema> {
        let path = format!(
            "/payments/subscriptions/{}",
            crate::services::encode(subscription_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// List Transactions
    ///
    /// The "List Transactions" API allows to retrieve a paginated list of transactions.
    /// Customize your results by filtering transactions based on name, alt type,
    /// transaction status, payment mode, date range, type of source, contact, subscription
    /// id, entity id or paginate through the list using the provided query parameters. This
    /// endpoint provides a straightforward way to explore and retrieve transaction
    /// information.
    ///
    /// `GET /payments/transactions`
    ///
    /// Requires scope: `payments/transactions.readonly`.
    pub async fn list_transactions(
        &self,
        params: &ListTransactionsParams,
    ) -> Result<models::ListTxnsResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/payments/transactions",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get Transaction by ID
    ///
    /// The "Get Transaction by ID" API allows to retrieve information for a specific
    /// transaction using its unique identifier. Use this endpoint to fetch details for a
    /// single transaction based on the provided transaction ID.
    ///
    /// `GET /payments/transactions/{transactionId}`
    ///
    /// Requires scope: `payments/transactions.readonly`.
    pub async fn get_transaction_by_id(
        &self,
        transaction_id: &str,
        params: &GetTransactionByIdParams,
    ) -> Result<models::GetTxnResponseSchema> {
        let path = format!(
            "/payments/transactions/{}",
            crate::services::encode(transaction_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }
}
