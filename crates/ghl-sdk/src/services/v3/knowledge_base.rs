// @generated by xtask/generate_services.py — do not edit by hand.
//! `knowledge-base` — typed methods for all 14 API v3 operations
//! in this module.
//!
//! Access via [`Ghl::v3`](crate::Ghl::v3)`().knowledge_base()`. These endpoints send `Version: v3`.
//!
//! Request and response types come from [`ghl_models::v3::knowledge_base`](https://docs.rs/ghl-models/latest/ghl_models/v3/knowledge_base/); every endpoint is also documented in the
//! [`knowledge-base` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/knowledge-base.md).
//!
//! Enable with `features = ["knowledge-base"]`.

#![allow(clippy::too_many_arguments)]

use crate::client::Ghl;
use crate::error::Result;
use ghl_models::v3::knowledge_base as models;

/// Typed access to the `knowledge-base` API v3 surface (14 operations). Obtained via
/// [`Ghl::v3`](crate::Ghl::v3)`().knowledge_base()`.
#[derive(Debug, Clone)]
pub struct KnowledgeBaseService {
    pub(crate) client: Ghl,
}

impl KnowledgeBaseService {
    pub(crate) fn new(client: Ghl) -> Self {
        Self { client }
    }
}

/// Query parameters for [`KnowledgeBaseService::get_knowledge_bases`].
#[derive(Debug, Clone, Default)]
pub struct GetKnowledgeBasesParams {
    /// `locationId` query parameter.
    /// Required by the API.
    pub location_id: String,
    /// search query for knowledge base name
    pub query: Option<String>,
    /// Maximum number of knowledge bases to return
    pub limit: Option<f64>,
    /// ID of the last knowledge base from the previous page (for pagination)
    pub last_knowledge_base_id: Option<String>,
}

impl GetKnowledgeBasesParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            ..Default::default()
        }
    }

    /// search query for knowledge base name
    pub fn query(mut self, v: impl Into<String>) -> Self {
        self.query = Some(v.into());
        self
    }

    /// Maximum number of knowledge bases to return
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// ID of the last knowledge base from the previous page (for pagination)
    pub fn last_knowledge_base_id(mut self, v: impl Into<String>) -> Self {
        self.last_knowledge_base_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        if let Some(v) = &self.query {
            q.push(("query".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.last_knowledge_base_id {
            q.push(("lastKnowledgeBaseId".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for
/// [`KnowledgeBaseService::get_all_trained_page_links_by_knowledge_base`].
#[derive(Debug, Clone, Default)]
pub struct GetAllTrainedPageLinksByKnowledgeBaseParams {
    /// knowledge base ID as string
    /// Required by the API.
    pub knowledge_base_id: String,
    /// location ID as string
    /// Required by the API.
    pub location_id: String,
    /// Page number
    pub page: Option<f64>,
    /// Records per page
    pub page_length: Option<f64>,
    /// query to filter on url links
    pub query: Option<String>,
}

impl GetAllTrainedPageLinksByKnowledgeBaseParams {
    /// Start from the parameters the API requires.
    pub fn new(knowledge_base_id: impl Into<String>, location_id: impl Into<String>) -> Self {
        Self {
            knowledge_base_id: knowledge_base_id.into(),
            location_id: location_id.into(),
            ..Default::default()
        }
    }

    /// Page number
    pub fn page(mut self, v: f64) -> Self {
        self.page = Some(v);
        self
    }

    /// Records per page
    pub fn page_length(mut self, v: f64) -> Self {
        self.page_length = Some(v);
        self
    }

    /// query to filter on url links
    pub fn query(mut self, v: impl Into<String>) -> Self {
        self.query = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("knowledgeBaseId".into(), self.knowledge_base_id.clone()),
            ("locationId".into(), self.location_id.clone()),
        ];
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        if let Some(v) = &self.page_length {
            q.push(("pageLength".into(), v.to_string()));
        }
        if let Some(v) = &self.query {
            q.push(("query".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for
/// [`KnowledgeBaseService::get_crawling_status_for_the_latest_operation`].
#[derive(Debug, Clone, Default)]
pub struct GetCrawlingStatusForTheLatestOperationParams {
    /// Location ID as string
    /// Required by the API.
    pub location_id: String,
    /// operation id as string
    /// Required by the API.
    pub operation_id: String,
    /// knowledge base id
    /// Required by the API.
    pub knowledge_base_id: String,
}

impl GetCrawlingStatusForTheLatestOperationParams {
    /// Start from the parameters the API requires.
    pub fn new(
        location_id: impl Into<String>,
        operation_id: impl Into<String>,
        knowledge_base_id: impl Into<String>,
    ) -> Self {
        Self {
            location_id: location_id.into(),
            operation_id: operation_id.into(),
            knowledge_base_id: knowledge_base_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("operationId".into(), self.operation_id.clone()),
            ("knowledgeBaseId".into(), self.knowledge_base_id.clone()),
        ];
        q
    }
}

/// Query parameters for
/// [`KnowledgeBaseService::get_all_fa_qs_by_knowledge_base_with_pagination_support`].
#[derive(Debug, Clone, Default)]
pub struct GetAllFaQsByKnowledgeBaseWithPaginationSupportParams {
    /// knowledge base ID as string
    /// Required by the API.
    pub knowledge_base_id: String,
    /// location ID as string
    /// Required by the API.
    pub location_id: String,
    /// Limit the number of FAQs returned
    pub limit: Option<f64>,
    /// Last FAQ ID for pagination (cursor-based)
    pub last_faq_id: Option<String>,
}

impl GetAllFaQsByKnowledgeBaseWithPaginationSupportParams {
    /// Start from the parameters the API requires.
    pub fn new(knowledge_base_id: impl Into<String>, location_id: impl Into<String>) -> Self {
        Self {
            knowledge_base_id: knowledge_base_id.into(),
            location_id: location_id.into(),
            ..Default::default()
        }
    }

    /// Limit the number of FAQs returned
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// Last FAQ ID for pagination (cursor-based)
    pub fn last_faq_id(mut self, v: impl Into<String>) -> Self {
        self.last_faq_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("knowledgeBaseId".into(), self.knowledge_base_id.clone()),
            ("locationId".into(), self.location_id.clone()),
        ];
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.last_faq_id {
            q.push(("lastFaqId".into(), v.to_string()));
        }
        q
    }
}

impl KnowledgeBaseService {
    /// Get all knowledge bases for a location by location Id (paginated)
    ///
    /// `GET /knowledge-bases/`
    pub async fn get_knowledge_bases(
        &self,
        params: &GetKnowledgeBasesParams,
    ) -> Result<models::GetAllKnowledgeBasesPaginatedResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/knowledge-bases/",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create a new knowledge base (max 15 knowledge bases per location)
    ///
    /// `POST /knowledge-bases/`
    pub async fn post_knowledge_bases(
        &self,
        body: &models::CreateKnowledgeBaseDTO,
    ) -> Result<models::CreateKnowledgeBaseResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/knowledge-bases/",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Delete trained pages
    ///
    /// `DELETE /knowledge-bases/crawler`
    pub async fn delete_trained_pages(
        &self,
        body: &models::DeleteWebsiteUrlRequestDTO,
    ) -> Result<models::DeleteWebsiteUrlResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                "/knowledge-bases/crawler",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Get all trained page links by knowledge base
    ///
    /// `GET /knowledge-bases/crawler`
    pub async fn get_all_trained_page_links_by_knowledge_base(
        &self,
        params: &GetAllTrainedPageLinksByKnowledgeBaseParams,
    ) -> Result<models::GetAllUrlsByKnowledgeBaseResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/knowledge-bases/crawler",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Start crawling and discover pages for training
    ///
    /// `POST /knowledge-bases/crawler`
    pub async fn start_crawling_and_discover_pages_for_training(
        &self,
        body: &models::DiscoverWebsiteRequestDTO,
    ) -> Result<models::DiscoverWebsiteResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/knowledge-bases/crawler",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Get crawling status for the latest operation
    ///
    /// `GET /knowledge-bases/crawler/status`
    pub async fn get_crawling_status_for_the_latest_operation(
        &self,
        params: &GetCrawlingStatusForTheLatestOperationParams,
    ) -> Result<models::CrawlingStatusResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/knowledge-bases/crawler/status",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Train discovered website pages and ingest into the knowledge base
    ///
    /// `POST /knowledge-bases/crawler/train`
    pub async fn post_knowledge_bases_crawler_train(
        &self,
        body: &models::TrainDiscoveredUrlsDTO,
    ) -> Result<models::TrainDiscoveredUrlsResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/knowledge-bases/crawler/train",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Get all FAQs by knowledge base with pagination support
    ///
    /// Retrieves FAQs for a knowledge base. Supports pagination using limit and lastFaqId
    /// parameters.
    ///
    /// `GET /knowledge-bases/faqs`
    pub async fn get_all_fa_qs_by_knowledge_base_with_pagination_support(
        &self,
        params: &GetAllFaQsByKnowledgeBaseWithPaginationSupportParams,
    ) -> Result<models::ListFaqsResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/knowledge-bases/faqs",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create a new FAQ inside knowledge base
    ///
    /// `POST /knowledge-bases/faqs`
    pub async fn create_a_new_faq_inside_knowledge_base(
        &self,
        body: &models::AddFaqDTO,
    ) -> Result<models::CreateFaqResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/knowledge-bases/faqs",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Delete an existing knowledge base FAQ
    ///
    /// `DELETE /knowledge-bases/faqs/{id}`
    pub async fn delete_an_existing_knowledge_base_faq(
        &self,
        id: &str,
    ) -> Result<models::DeleteFaqResponseDTO> {
        let path = format!("/knowledge-bases/faqs/{}", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Update an existing knowledge base FAQ
    ///
    /// `PUT /knowledge-bases/faqs/{id}`
    pub async fn update_an_existing_knowledge_base_faq(
        &self,
        id: &str,
        body: &models::UpdateFaqBodyDTO,
    ) -> Result<models::UpdateFaqResponseDTO> {
        let path = format!("/knowledge-bases/faqs/{}", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Update a knowledge base
    ///
    /// `PUT /knowledge-bases/{id}`
    pub async fn update_a_knowledge_base(
        &self,
        id: &str,
        body: &models::UpdateKnowledgeBaseDTO,
    ) -> Result<models::UpdateKnowledgeBaseResponseDTO> {
        let path = format!("/knowledge-bases/{}", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Delete a knowledge base
    ///
    /// `DELETE /knowledge-bases/{knowledgeBaseId}`
    pub async fn delete_a_knowledge_base(
        &self,
        knowledge_base_id: &str,
    ) -> Result<models::DeleteKnowledgeBaseResponseDTO> {
        let path = format!(
            "/knowledge-bases/{}",
            crate::services::encode(knowledge_base_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get knowledge base by ID
    ///
    /// `GET /knowledge-bases/{knowledgeBaseId}`
    pub async fn get_knowledge_base_by_id(
        &self,
        knowledge_base_id: &str,
    ) -> Result<models::GetKnowledgeBaseByIdResponseDTO> {
        let path = format!(
            "/knowledge-bases/{}",
            crate::services::encode(knowledge_base_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }
}
