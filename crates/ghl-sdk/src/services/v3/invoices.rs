// @generated by xtask/generate_services.py — do not edit by hand.
//! `invoices` — typed methods for all 42 API v3 operations
//! in this module.
//!
//! Access via [`Ghl::v3`](crate::Ghl::v3)`().invoices()`. These endpoints send `Version: v3`.
//!
//! Request and response types come from [`ghl_models::v3::invoices`](https://docs.rs/ghl-models/latest/ghl_models/v3/invoices/); every endpoint is also documented in the
//! [`invoices` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/invoices.md).
//!
//! Enable with `features = ["invoices"]`.

#![allow(clippy::too_many_arguments)]

use crate::client::Ghl;
use crate::error::Result;
use ghl_models::v3::invoices as models;

/// Typed access to the `invoices` API v3 surface (42 operations). Obtained via
/// [`Ghl::v3`](crate::Ghl::v3)`().invoices()`.
#[derive(Debug, Clone)]
pub struct InvoicesService {
    pub(crate) client: Ghl,
}

impl InvoicesService {
    pub(crate) fn new(client: Ghl) -> Self {
        Self { client }
    }
}

/// Query parameters for [`InvoicesService::list_invoices`].
#[derive(Debug, Clone, Default)]
pub struct ListInvoicesParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// status to be filtered
    pub status: Option<String>,
    /// startAt in YYYY-MM-DD format
    pub start_at: Option<String>,
    /// endAt in YYYY-MM-DD format
    pub end_at: Option<String>,
    /// To search for an invoice by id / name / email / phoneNo
    pub search: Option<String>,
    /// payment mode
    /// Allowed values: `default`, `live`, `test`.
    pub payment_mode: Option<String>,
    /// Contact ID for the invoice
    pub contact_id: Option<String>,
    /// Limit the number of items to return
    /// Required by the API.
    pub limit: String,
    /// Number of items to skip
    /// Required by the API.
    pub offset: String,
    /// The field on which sorting should be applied
    /// Allowed values: `issueDate`.
    pub sort_field: Option<String>,
    /// The order of sort which should be applied for the sortField
    /// Allowed values: `ascend`, `descend`.
    pub sort_order: Option<String>,
}

impl ListInvoicesParams {
    /// Start from the parameters the API requires.
    pub fn new(
        alt_id: impl Into<String>,
        alt_type: impl Into<String>,
        limit: impl Into<String>,
        offset: impl Into<String>,
    ) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            limit: limit.into(),
            offset: offset.into(),
            ..Default::default()
        }
    }

    /// status to be filtered
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    /// startAt in YYYY-MM-DD format
    pub fn start_at(mut self, v: impl Into<String>) -> Self {
        self.start_at = Some(v.into());
        self
    }

    /// endAt in YYYY-MM-DD format
    pub fn end_at(mut self, v: impl Into<String>) -> Self {
        self.end_at = Some(v.into());
        self
    }

    /// To search for an invoice by id / name / email / phoneNo
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// payment mode
    pub fn payment_mode(mut self, v: impl Into<String>) -> Self {
        self.payment_mode = Some(v.into());
        self
    }

    /// Contact ID for the invoice
    pub fn contact_id(mut self, v: impl Into<String>) -> Self {
        self.contact_id = Some(v.into());
        self
    }

    /// The field on which sorting should be applied
    pub fn sort_field(mut self, v: impl Into<String>) -> Self {
        self.sort_field = Some(v.into());
        self
    }

    /// The order of sort which should be applied for the sortField
    pub fn sort_order(mut self, v: impl Into<String>) -> Self {
        self.sort_order = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
            ("limit".into(), self.limit.clone()),
            ("offset".into(), self.offset.clone()),
        ];
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        if let Some(v) = &self.start_at {
            q.push(("startAt".into(), v.to_string()));
        }
        if let Some(v) = &self.end_at {
            q.push(("endAt".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.payment_mode {
            q.push(("paymentMode".into(), v.to_string()));
        }
        if let Some(v) = &self.contact_id {
            q.push(("contactId".into(), v.to_string()));
        }
        if let Some(v) = &self.sort_field {
            q.push(("sortField".into(), v.to_string()));
        }
        if let Some(v) = &self.sort_order {
            q.push(("sortOrder".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`InvoicesService::list_estimates`].
#[derive(Debug, Clone, Default)]
pub struct ListEstimatesParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// startAt in YYYY-MM-DD format
    pub start_at: Option<String>,
    /// endAt in YYYY-MM-DD format
    pub end_at: Option<String>,
    /// search text for estimates name
    pub search: Option<String>,
    /// estimate status
    /// Allowed values: `all`, `draft`, `sent`, `accepted`, `declined`, `invoiced`,
    /// `viewed`.
    pub status: Option<String>,
    /// Contact ID for the estimate
    pub contact_id: Option<String>,
    /// Limit the number of items to return
    /// Required by the API.
    pub limit: String,
    /// Number of items to skip
    /// Required by the API.
    pub offset: String,
}

impl ListEstimatesParams {
    /// Start from the parameters the API requires.
    pub fn new(
        alt_id: impl Into<String>,
        alt_type: impl Into<String>,
        limit: impl Into<String>,
        offset: impl Into<String>,
    ) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            limit: limit.into(),
            offset: offset.into(),
            ..Default::default()
        }
    }

    /// startAt in YYYY-MM-DD format
    pub fn start_at(mut self, v: impl Into<String>) -> Self {
        self.start_at = Some(v.into());
        self
    }

    /// endAt in YYYY-MM-DD format
    pub fn end_at(mut self, v: impl Into<String>) -> Self {
        self.end_at = Some(v.into());
        self
    }

    /// search text for estimates name
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// estimate status
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    /// Contact ID for the estimate
    pub fn contact_id(mut self, v: impl Into<String>) -> Self {
        self.contact_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
            ("limit".into(), self.limit.clone()),
            ("offset".into(), self.offset.clone()),
        ];
        if let Some(v) = &self.start_at {
            q.push(("startAt".into(), v.to_string()));
        }
        if let Some(v) = &self.end_at {
            q.push(("endAt".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        if let Some(v) = &self.contact_id {
            q.push(("contactId".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`InvoicesService::generate_estimate_number`].
#[derive(Debug, Clone, Default)]
pub struct GenerateEstimateNumberParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GenerateEstimateNumberParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`InvoicesService::list_estimate_templates`].
#[derive(Debug, Clone, Default)]
pub struct ListEstimateTemplatesParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// To search for an estimate template by id / name
    pub search: Option<String>,
    /// Limit the number of items to return
    /// Required by the API.
    pub limit: String,
    /// Number of items to skip
    /// Required by the API.
    pub offset: String,
}

impl ListEstimateTemplatesParams {
    /// Start from the parameters the API requires.
    pub fn new(
        alt_id: impl Into<String>,
        alt_type: impl Into<String>,
        limit: impl Into<String>,
        offset: impl Into<String>,
    ) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            limit: limit.into(),
            offset: offset.into(),
            ..Default::default()
        }
    }

    /// To search for an estimate template by id / name
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
            ("limit".into(), self.limit.clone()),
            ("offset".into(), self.offset.clone()),
        ];
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`InvoicesService::preview_estimate_template`].
#[derive(Debug, Clone, Default)]
pub struct PreviewEstimateTemplateParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// Template Id
    /// Required by the API.
    pub template_id: String,
}

impl PreviewEstimateTemplateParams {
    /// Start from the parameters the API requires.
    pub fn new(
        alt_id: impl Into<String>,
        alt_type: impl Into<String>,
        template_id: impl Into<String>,
    ) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            template_id: template_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
            ("templateId".into(), self.template_id.clone()),
        ];
        q
    }
}

/// Query parameters for [`InvoicesService::generate_invoice_number`].
#[derive(Debug, Clone, Default)]
pub struct GenerateInvoiceNumberParams {
    /// Location Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GenerateInvoiceNumberParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`InvoicesService::list_schedules`].
#[derive(Debug, Clone, Default)]
pub struct ListSchedulesParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// status to be filtered
    pub status: Option<String>,
    /// startAt in YYYY-MM-DD format
    pub start_at: Option<String>,
    /// endAt in YYYY-MM-DD format
    pub end_at: Option<String>,
    /// To search for an invoice by id / name / email / phoneNo
    pub search: Option<String>,
    /// payment mode
    /// Allowed values: `default`, `live`, `test`.
    pub payment_mode: Option<String>,
    /// Limit the number of items to return
    /// Required by the API.
    pub limit: String,
    /// Number of items to skip
    /// Required by the API.
    pub offset: String,
}

impl ListSchedulesParams {
    /// Start from the parameters the API requires.
    pub fn new(
        alt_id: impl Into<String>,
        alt_type: impl Into<String>,
        limit: impl Into<String>,
        offset: impl Into<String>,
    ) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            limit: limit.into(),
            offset: offset.into(),
            ..Default::default()
        }
    }

    /// status to be filtered
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    /// startAt in YYYY-MM-DD format
    pub fn start_at(mut self, v: impl Into<String>) -> Self {
        self.start_at = Some(v.into());
        self
    }

    /// endAt in YYYY-MM-DD format
    pub fn end_at(mut self, v: impl Into<String>) -> Self {
        self.end_at = Some(v.into());
        self
    }

    /// To search for an invoice by id / name / email / phoneNo
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// payment mode
    pub fn payment_mode(mut self, v: impl Into<String>) -> Self {
        self.payment_mode = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
            ("limit".into(), self.limit.clone()),
            ("offset".into(), self.offset.clone()),
        ];
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        if let Some(v) = &self.start_at {
            q.push(("startAt".into(), v.to_string()));
        }
        if let Some(v) = &self.end_at {
            q.push(("endAt".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.payment_mode {
            q.push(("paymentMode".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`InvoicesService::delete_schedule`].
#[derive(Debug, Clone, Default)]
pub struct DeleteScheduleParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl DeleteScheduleParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`InvoicesService::get_an_schedule`].
#[derive(Debug, Clone, Default)]
pub struct GetAnScheduleParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GetAnScheduleParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`InvoicesService::get_invoice_settings`].
#[derive(Debug, Clone, Default)]
pub struct GetInvoiceSettingsParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GetInvoiceSettingsParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`InvoicesService::list_templates`].
#[derive(Debug, Clone, Default)]
pub struct ListTemplatesParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// status to be filtered
    pub status: Option<String>,
    /// startAt in YYYY-MM-DD format
    pub start_at: Option<String>,
    /// endAt in YYYY-MM-DD format
    pub end_at: Option<String>,
    /// To search for an invoice by id / name / email / phoneNo
    pub search: Option<String>,
    /// payment mode
    /// Allowed values: `default`, `live`, `test`.
    pub payment_mode: Option<String>,
    /// Limit the number of items to return
    /// Required by the API.
    pub limit: String,
    /// Number of items to skip
    /// Required by the API.
    pub offset: String,
}

impl ListTemplatesParams {
    /// Start from the parameters the API requires.
    pub fn new(
        alt_id: impl Into<String>,
        alt_type: impl Into<String>,
        limit: impl Into<String>,
        offset: impl Into<String>,
    ) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            limit: limit.into(),
            offset: offset.into(),
            ..Default::default()
        }
    }

    /// status to be filtered
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    /// startAt in YYYY-MM-DD format
    pub fn start_at(mut self, v: impl Into<String>) -> Self {
        self.start_at = Some(v.into());
        self
    }

    /// endAt in YYYY-MM-DD format
    pub fn end_at(mut self, v: impl Into<String>) -> Self {
        self.end_at = Some(v.into());
        self
    }

    /// To search for an invoice by id / name / email / phoneNo
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// payment mode
    pub fn payment_mode(mut self, v: impl Into<String>) -> Self {
        self.payment_mode = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
            ("limit".into(), self.limit.clone()),
            ("offset".into(), self.offset.clone()),
        ];
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        if let Some(v) = &self.start_at {
            q.push(("startAt".into(), v.to_string()));
        }
        if let Some(v) = &self.end_at {
            q.push(("endAt".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.payment_mode {
            q.push(("paymentMode".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`InvoicesService::delete_template`].
#[derive(Debug, Clone, Default)]
pub struct DeleteTemplateParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl DeleteTemplateParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`InvoicesService::get_an_template`].
#[derive(Debug, Clone, Default)]
pub struct GetAnTemplateParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GetAnTemplateParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`InvoicesService::delete_invoice`].
#[derive(Debug, Clone, Default)]
pub struct DeleteInvoiceParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl DeleteInvoiceParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`InvoicesService::get_invoice`].
#[derive(Debug, Clone, Default)]
pub struct GetInvoiceParams {
    /// location Id / company Id based on altType
    /// Required by the API.
    pub alt_id: String,
    /// Alt Type
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GetInvoiceParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

impl InvoicesService {
    /// List invoices
    ///
    /// API to get list of invoices
    ///
    /// `GET /invoices/`
    ///
    /// Requires scope: `invoices.readonly`.
    pub async fn list_invoices(
        &self,
        params: &ListInvoicesParams,
    ) -> Result<models::ListInvoicesResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/invoices/",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create Invoice
    ///
    /// API to create an invoice
    ///
    /// `POST /invoices/`
    ///
    /// Requires scope: `invoices.write`.
    pub async fn create_invoice(
        &self,
        body: &models::CreateInvoiceDto,
    ) -> Result<models::CreateInvoiceResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/invoices/",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Create New Estimate
    ///
    /// Create a new estimate with the provided details
    ///
    /// `POST /invoices/estimate`
    ///
    /// Requires scope: `invoices/estimate.write`.
    pub async fn create_new_estimate(
        &self,
        body: &models::CreateEstimatesDto,
    ) -> Result<models::EstimateResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/invoices/estimate",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// List Estimates
    ///
    /// Get a paginated list of estimates
    ///
    /// `GET /invoices/estimate/list`
    ///
    /// Requires scope: `invoices/estimate.readonly`.
    pub async fn list_estimates(
        &self,
        params: &ListEstimatesParams,
    ) -> Result<models::ListEstimatesResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/invoices/estimate/list",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Generate Estimate Number
    ///
    /// Get the next estimate number for the given location
    ///
    /// `GET /invoices/estimate/number/generate`
    ///
    /// Requires scope: `invoices/estimate.readonly`.
    pub async fn generate_estimate_number(
        &self,
        params: &GenerateEstimateNumberParams,
    ) -> Result<models::GenerateEstimateNumberResponse> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/invoices/estimate/number/generate",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Update estimate last visited at
    ///
    /// API to update estimate last visited at by estimate id
    ///
    /// `PATCH /invoices/estimate/stats/last-visited-at`
    pub async fn update_estimate_last_visited_at(
        &self,
        body: &models::EstimateIdParam,
    ) -> Result<serde_json::Value> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PATCH,
                "/invoices/estimate/stats/last-visited-at",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// List Estimate Templates
    ///
    /// Get a list of estimate templates or a specific template by ID
    ///
    /// `GET /invoices/estimate/template`
    ///
    /// Requires scope: `invoices/estimate.readonly`.
    pub async fn list_estimate_templates(
        &self,
        params: &ListEstimateTemplatesParams,
    ) -> Result<models::ListEstimateTemplateResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/invoices/estimate/template",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create Estimate Template
    ///
    /// Create a new estimate template
    ///
    /// `POST /invoices/estimate/template`
    ///
    /// Requires scope: `invoices/estimate.write`.
    pub async fn create_estimate_template(
        &self,
        body: &models::EstimateTemplatesDto,
    ) -> Result<models::EstimateTemplateResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/invoices/estimate/template",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Preview Estimate Template
    ///
    /// Get a preview of an estimate template
    ///
    /// `GET /invoices/estimate/template/preview`
    ///
    /// Requires scope: `invoices/estimate.readonly`.
    pub async fn preview_estimate_template(
        &self,
        params: &PreviewEstimateTemplateParams,
    ) -> Result<models::EstimateTemplateResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/invoices/estimate/template/preview",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Delete Estimate Template
    ///
    /// Delete an existing estimate template
    ///
    /// `DELETE /invoices/estimate/template/{templateId}`
    ///
    /// Requires scope: `invoices/estimate.write`.
    pub async fn delete_estimate_template(
        &self,
        template_id: &str,
        body: &models::AltDto,
    ) -> Result<models::EstimateTemplateResponseDTO> {
        let path = format!(
            "/invoices/estimate/template/{}",
            crate::services::encode(template_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Update Estimate Template
    ///
    /// Update an existing estimate template
    ///
    /// `PUT /invoices/estimate/template/{templateId}`
    ///
    /// Requires scope: `invoices/estimate.write`.
    pub async fn update_estimate_template(
        &self,
        template_id: &str,
        body: &models::EstimateTemplatesDto,
    ) -> Result<models::EstimateTemplateResponseDTO> {
        let path = format!(
            "/invoices/estimate/template/{}",
            crate::services::encode(template_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Delete Estimate
    ///
    /// Delete an existing estimate
    ///
    /// `DELETE /invoices/estimate/{estimateId}`
    ///
    /// Requires scope: `invoices/estimate.write`.
    pub async fn delete_estimate(
        &self,
        estimate_id: &str,
        body: &models::AltDto,
    ) -> Result<models::EstimateResponseDto> {
        let path = format!(
            "/invoices/estimate/{}",
            crate::services::encode(estimate_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Update Estimate
    ///
    /// Update an existing estimate with new details
    ///
    /// `PUT /invoices/estimate/{estimateId}`
    ///
    /// Requires scope: `invoices/estimate.write`.
    pub async fn update_estimate(
        &self,
        estimate_id: &str,
        body: &models::UpdateEstimateDto,
    ) -> Result<models::EstimateResponseDto> {
        let path = format!(
            "/invoices/estimate/{}",
            crate::services::encode(estimate_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Create Invoice from Estimate
    ///
    /// Create a new invoice from an existing estimate
    ///
    /// `POST /invoices/estimate/{estimateId}/invoice`
    ///
    /// Requires scope: `invoices/estimate.write`.
    pub async fn create_invoice_from_estimate(
        &self,
        estimate_id: &str,
        body: &models::CreateInvoiceFromEstimateDto,
    ) -> Result<models::CreateInvoiceFromEstimateResponseDTO> {
        let path = format!(
            "/invoices/estimate/{}/invoice",
            crate::services::encode(estimate_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Send Estimate
    ///
    /// API to send estimate by estimate id
    ///
    /// `POST /invoices/estimate/{estimateId}/send`
    ///
    /// Requires scope: `invoices/estimate.write`.
    pub async fn send_estimate(
        &self,
        estimate_id: &str,
        body: &models::SendEstimateDto,
    ) -> Result<models::EstimateResponseDto> {
        let path = format!(
            "/invoices/estimate/{}/send",
            crate::services::encode(estimate_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Generate Invoice Number
    ///
    /// Get the next invoice number for the given location
    ///
    /// `GET /invoices/generate-invoice-number`
    ///
    /// Requires scope: `invoices.readonly`.
    pub async fn generate_invoice_number(
        &self,
        params: &GenerateInvoiceNumberParams,
    ) -> Result<models::GenerateInvoiceNumberResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/invoices/generate-invoice-number",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// List schedules
    ///
    /// API to get list of schedules
    ///
    /// `GET /invoices/schedule`
    ///
    /// Requires scope: `invoices/schedule.readonly`.
    pub async fn list_schedules(
        &self,
        params: &ListSchedulesParams,
    ) -> Result<models::ListSchedulesResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/invoices/schedule",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create Invoice Schedule
    ///
    /// API to create an invoice Schedule
    ///
    /// `POST /invoices/schedule`
    ///
    /// Requires scope: `invoices/schedule.write`.
    pub async fn create_invoice_schedule(
        &self,
        body: &models::CreateInvoiceScheduleDto,
    ) -> Result<models::CreateInvoiceScheduleResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/invoices/schedule",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Delete schedule
    ///
    /// API to delete an schedule by schedule id
    ///
    /// `DELETE /invoices/schedule/{scheduleId}`
    ///
    /// Requires scope: `invoices/schedule.write`.
    pub async fn delete_schedule(
        &self,
        schedule_id: &str,
        params: &DeleteScheduleParams,
    ) -> Result<models::DeleteInvoiceScheduleResponseDto> {
        let path = format!(
            "/invoices/schedule/{}",
            crate::services::encode(schedule_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get an schedule
    ///
    /// API to get an schedule by schedule id
    ///
    /// `GET /invoices/schedule/{scheduleId}`
    ///
    /// Requires scope: `invoices/schedule.readonly`.
    pub async fn get_an_schedule(
        &self,
        schedule_id: &str,
        params: &GetAnScheduleParams,
    ) -> Result<models::GetScheduleResponseDto> {
        let path = format!(
            "/invoices/schedule/{}",
            crate::services::encode(schedule_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Update schedule
    ///
    /// API to update an schedule by schedule id
    ///
    /// `PUT /invoices/schedule/{scheduleId}`
    ///
    /// Requires scope: `invoices/schedule.write`.
    pub async fn update_schedule(
        &self,
        schedule_id: &str,
        body: &models::UpdateInvoiceScheduleDto,
    ) -> Result<models::UpdateInvoiceScheduleResponseDto> {
        let path = format!(
            "/invoices/schedule/{}",
            crate::services::encode(schedule_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Manage Auto payment for an schedule invoice
    ///
    /// API to manage auto payment for a schedule
    ///
    /// `POST /invoices/schedule/{scheduleId}/auto-payment`
    ///
    /// Requires scope: `invoices/schedule.write`.
    pub async fn manage_auto_payment_for_an_schedule_invoice(
        &self,
        schedule_id: &str,
        body: &models::AutoPaymentScheduleDto,
    ) -> Result<models::AutoPaymentInvoiceScheduleResponseDto> {
        let path = format!(
            "/invoices/schedule/{}/auto-payment",
            crate::services::encode(schedule_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Cancel an scheduled invoice
    ///
    /// API to cancel a scheduled invoice by schedule id
    ///
    /// `POST /invoices/schedule/{scheduleId}/cancel`
    ///
    /// Requires scope: `invoices/schedule.write`.
    pub async fn cancel_an_scheduled_invoice(
        &self,
        schedule_id: &str,
        body: &models::CancelInvoiceScheduleDto,
    ) -> Result<models::CancelInvoiceScheduleResponseDto> {
        let path = format!(
            "/invoices/schedule/{}/cancel",
            crate::services::encode(schedule_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Schedule an schedule invoice
    ///
    /// API to schedule an schedule invoice to start sending to the customer
    ///
    /// `POST /invoices/schedule/{scheduleId}/schedule`
    ///
    /// Requires scope: `invoices/schedule.write`.
    pub async fn schedule_an_schedule_invoice(
        &self,
        schedule_id: &str,
        body: &models::ScheduleInvoiceScheduleDto,
    ) -> Result<models::ScheduleInvoiceScheduleResponseDto> {
        let path = format!(
            "/invoices/schedule/{}/schedule",
            crate::services::encode(schedule_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Update scheduled recurring invoice
    ///
    /// API to update scheduled recurring invoice
    ///
    /// `POST /invoices/schedule/{scheduleId}/updateAndSchedule`
    ///
    /// Requires scope: `invoices/schedule.write`.
    pub async fn update_scheduled_recurring_invoice(
        &self,
        schedule_id: &str,
    ) -> Result<models::UpdateAndScheduleInvoiceScheduleResponseDto> {
        let path = format!(
            "/invoices/schedule/{}/updateAndSchedule",
            crate::services::encode(schedule_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get Invoice Settings
    ///
    /// Get the invoice settings for the given location
    ///
    /// `GET /invoices/settings`
    ///
    /// Requires scope: `invoices.readonly`.
    pub async fn get_invoice_settings(
        &self,
        params: &GetInvoiceSettingsParams,
    ) -> Result<models::GetInvoiceSettingsResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/invoices/settings",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Update invoice last visited at
    ///
    /// API to update invoice last visited at by invoice id
    ///
    /// `PATCH /invoices/stats/last-visited-at`
    pub async fn update_invoice_last_visited_at(
        &self,
        body: &models::PatchInvoiceStatsLastViewedDto,
    ) -> Result<serde_json::Value> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PATCH,
                "/invoices/stats/last-visited-at",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// List templates
    ///
    /// API to get list of templates
    ///
    /// `GET /invoices/template`
    ///
    /// Requires scope: `invoices/template.readonly`.
    pub async fn list_templates(
        &self,
        params: &ListTemplatesParams,
    ) -> Result<models::ListTemplatesResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/invoices/template",
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Create template
    ///
    /// API to create a template
    ///
    /// `POST /invoices/template`
    ///
    /// Requires scope: `invoices/template.write`.
    pub async fn create_template(
        &self,
        body: &models::CreateInvoiceTemplateDto,
    ) -> Result<models::CreateInvoiceTemplateResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/invoices/template",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Delete template
    ///
    /// API to update an template by template id
    ///
    /// `DELETE /invoices/template/{templateId}`
    ///
    /// Requires scope: `invoices/template.write`.
    pub async fn delete_template(
        &self,
        template_id: &str,
        params: &DeleteTemplateParams,
    ) -> Result<models::DeleteInvoiceTemplateResponseDto> {
        let path = format!(
            "/invoices/template/{}",
            crate::services::encode(template_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get an template
    ///
    /// API to get an template by template id
    ///
    /// `GET /invoices/template/{templateId}`
    ///
    /// Requires scope: `invoices/template.readonly`.
    pub async fn get_an_template(
        &self,
        template_id: &str,
        params: &GetAnTemplateParams,
    ) -> Result<models::GetTemplateResponseDto> {
        let path = format!(
            "/invoices/template/{}",
            crate::services::encode(template_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Update template
    ///
    /// API to update an template by template id
    ///
    /// `PUT /invoices/template/{templateId}`
    ///
    /// Requires scope: `invoices/template.write`.
    pub async fn update_template(
        &self,
        template_id: &str,
        body: &models::UpdateInvoiceTemplateDto,
    ) -> Result<models::UpdateInvoiceTemplateResponseDto> {
        let path = format!(
            "/invoices/template/{}",
            crate::services::encode(template_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Update template late fees configuration
    ///
    /// API to update template late fees configuration by template id
    ///
    /// `PATCH /invoices/template/{templateId}/late-fees-configuration`
    pub async fn update_template_late_fees_configuration(
        &self,
        template_id: &str,
        body: &models::UpdateInvoiceLateFeesConfigurationDto,
    ) -> Result<models::UpdateInvoiceTemplateResponseDto> {
        let path = format!(
            "/invoices/template/{}/late-fees-configuration",
            crate::services::encode(template_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PATCH,
                &path,
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Update template late fees configuration
    ///
    /// API to update template late fees configuration by template id
    ///
    /// `PATCH /invoices/template/{templateId}/payment-methods-configuration`
    pub async fn update_template_late_fees_configuration_op(
        &self,
        template_id: &str,
        body: &models::UpdatePaymentMethodsConfigurationDto,
    ) -> Result<models::UpdateInvoiceTemplateResponseDto> {
        let path = format!(
            "/invoices/template/{}/payment-methods-configuration",
            crate::services::encode(template_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PATCH,
                &path,
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Create & Send
    ///
    /// API to create or update a text2pay invoice
    ///
    /// `POST /invoices/text2pay`
    ///
    /// Requires scope: `invoices.write`.
    pub async fn create_send(
        &self,
        body: &models::Text2PayDto,
    ) -> Result<models::Text2PayInvoiceResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/invoices/text2pay",
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Delete invoice
    ///
    /// API to delete invoice by invoice id
    ///
    /// `DELETE /invoices/{invoiceId}`
    ///
    /// Requires scope: `invoices.write`.
    pub async fn delete_invoice(
        &self,
        invoice_id: &str,
        params: &DeleteInvoiceParams,
    ) -> Result<models::DeleteInvoiceResponseDto> {
        let path = format!("/invoices/{}", crate::services::encode(invoice_id));
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get invoice
    ///
    /// API to get invoice by invoice id
    ///
    /// `GET /invoices/{invoiceId}`
    ///
    /// Requires scope: `invoices.readonly`.
    pub async fn get_invoice(
        &self,
        invoice_id: &str,
        params: &GetInvoiceParams,
    ) -> Result<models::GetInvoiceResponseDto> {
        let path = format!("/invoices/{}", crate::services::encode(invoice_id));
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Update invoice
    ///
    /// API to update invoice by invoice id
    ///
    /// `PUT /invoices/{invoiceId}`
    ///
    /// Requires scope: `invoices.write`.
    pub async fn update_invoice(
        &self,
        invoice_id: &str,
        body: &models::UpdateInvoiceDto,
    ) -> Result<models::UpdateInvoiceResponseDto> {
        let path = format!("/invoices/{}", crate::services::encode(invoice_id));
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Update invoice late fees configuration
    ///
    /// API to update invoice late fees configuration by invoice id
    ///
    /// `PATCH /invoices/{invoiceId}/late-fees-configuration`
    pub async fn update_invoice_late_fees_configuration(
        &self,
        invoice_id: &str,
        body: &models::UpdateInvoiceLateFeesConfigurationDto,
    ) -> Result<models::UpdateInvoiceResponseDto> {
        let path = format!(
            "/invoices/{}/late-fees-configuration",
            crate::services::encode(invoice_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PATCH,
                &path,
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Record a manual payment for an invoice
    ///
    /// API to record manual payment for an invoice by invoice id
    ///
    /// `POST /invoices/{invoiceId}/record-payment`
    ///
    /// Requires scope: `invoices.write`.
    pub async fn record_a_manual_payment_for_an_invoice(
        &self,
        invoice_id: &str,
        body: &models::RecordPaymentDto,
    ) -> Result<models::RecordPaymentResponseDto> {
        let path = format!(
            "/invoices/{}/record-payment",
            crate::services::encode(invoice_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Send invoice
    ///
    /// API to send invoice by invoice id
    ///
    /// `POST /invoices/{invoiceId}/send`
    ///
    /// Requires scope: `invoices.write`.
    pub async fn send_invoice(
        &self,
        invoice_id: &str,
        body: &models::SendInvoiceDto,
    ) -> Result<models::SendInvoicesResponseDto> {
        let path = format!("/invoices/{}/send", crate::services::encode(invoice_id));
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Void invoice
    ///
    /// API to delete invoice by invoice id
    ///
    /// `POST /invoices/{invoiceId}/void`
    ///
    /// Requires scope: `invoices.write`.
    pub async fn void_invoice(
        &self,
        invoice_id: &str,
        body: &models::VoidInvoiceDto,
    ) -> Result<models::VoidInvoiceResponseDto> {
        let path = format!("/invoices/{}/void", crate::services::encode(invoice_id));
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }
}
