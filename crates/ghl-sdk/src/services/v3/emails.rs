// @generated by xtask/generate_services.py — do not edit by hand.
//! `emails` — typed methods for all 18 API v3 operations
//! in this module.
//!
//! Access via [`Ghl::v3`](crate::Ghl::v3)`().emails()`. These endpoints send `Version: v3`.
//!
//! Request and response types come from [`ghl_models::v3::emails`](https://docs.rs/ghl-models/latest/ghl_models/v3/emails/); every endpoint is also documented in the
//! [`emails` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/emails.md).
//!
//! Enable with `features = ["emails"]`.

#![allow(clippy::too_many_arguments)]

use crate::client::Ghl;
use crate::error::Result;
use ghl_models::v3::emails as models;

/// Typed access to the `emails` API v3 surface (18 operations). Obtained via
/// [`Ghl::v3`](crate::Ghl::v3)`().emails()`.
#[derive(Debug, Clone)]
pub struct EmailsService {
    pub(crate) client: Ghl,
}

impl EmailsService {
    pub(crate) fn new(client: Ghl) -> Self {
        Self { client }
    }
}

/// Query parameters for [`EmailsService::list_bulk_action_campaigns`].
#[derive(Debug, Clone, Default)]
pub struct ListBulkActionCampaignsParams {
    /// Number of campaigns to return. Defaults to 10, minimum is 1, maximum is 20
    pub limit: Option<f64>,
    /// Number of campaigns to skip for pagination. Defaults to 0, minimum is 0
    pub offset: Option<f64>,
    /// Search query to filter campaigns.
    pub search: Option<String>,
    /// Filter by start date (ISO 8601 format)
    pub date_from: Option<String>,
    /// Filter by end date (ISO 8601 format)
    pub date_to: Option<String>,
    /// Filter by status
    /// Allowed values: `processing`, `scheduled`, `paused`, `complete`, `cancelled`.
    pub status: Option<String>,
}

impl ListBulkActionCampaignsParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Number of campaigns to return. Defaults to 10, minimum is 1, maximum is 20
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// Number of campaigns to skip for pagination. Defaults to 0, minimum is 0
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    /// Search query to filter campaigns.
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// Filter by start date (ISO 8601 format)
    pub fn date_from(mut self, v: impl Into<String>) -> Self {
        self.date_from = Some(v.into());
        self
    }

    /// Filter by end date (ISO 8601 format)
    pub fn date_to(mut self, v: impl Into<String>) -> Self {
        self.date_to = Some(v.into());
        self
    }

    /// Filter by status
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.date_from {
            q.push(("dateFrom".into(), v.to_string()));
        }
        if let Some(v) = &self.date_to {
            q.push(("dateTo".into(), v.to_string()));
        }
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`EmailsService::list_email_campaigns`].
#[derive(Debug, Clone, Default)]
pub struct ListEmailCampaignsParams {
    /// Number of campaigns to return. Defaults to 10, minimum is 1, maximum is 20
    pub limit: Option<f64>,
    /// Number of campaigns to skip for pagination. Defaults to 0, minimum is 0
    pub offset: Option<f64>,
    /// Search text for campaign name
    pub search: Option<String>,
    /// Filter by campaign status
    /// Allowed values: `all`, `sent`, `failed`, `archived`, `draft`, `processing`,
    /// `scheduled`, `cancelled`, `paused`.
    pub status: Option<String>,
}

impl ListEmailCampaignsParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Number of campaigns to return. Defaults to 10, minimum is 1, maximum is 20
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// Number of campaigns to skip for pagination. Defaults to 0, minimum is 0
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    /// Search text for campaign name
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// Filter by campaign status
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`EmailsService::get_campaign_statistics`].
#[derive(Debug, Clone, Default)]
pub struct GetCampaignStatisticsParams {
    /// Workflow action ID. Only valid when source is `workflow-campaigns`
    pub sub_source_id: Option<String>,
}

impl GetCampaignStatisticsParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Workflow action ID. Only valid when source is `workflow-campaigns`
    pub fn sub_source_id(mut self, v: impl Into<String>) -> Self {
        self.sub_source_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.sub_source_id {
            q.push(("subSourceId".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`EmailsService::list_workflow_campaigns`].
#[derive(Debug, Clone, Default)]
pub struct ListWorkflowCampaignsParams {
    /// Number of campaigns to return. Defaults to 10, minimum is 1, maximum is 20
    pub limit: Option<f64>,
    /// Number of items to skip for pagination. Defaults to 0, minimum is 0
    pub offset: Option<f64>,
    /// Search query to filter campaigns.
    pub search: Option<String>,
    /// Filter by campaign status
    /// Allowed values: `published`, `draft`.
    pub status: Option<String>,
}

impl ListWorkflowCampaignsParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Number of campaigns to return. Defaults to 10, minimum is 1, maximum is 20
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// Number of items to skip for pagination. Defaults to 0, minimum is 0
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    /// Search query to filter campaigns.
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// Filter by campaign status
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`EmailsService::list_templates`].
#[derive(Debug, Clone, Default)]
pub struct ListTemplatesParams {
    /// Number of templates to return
    pub limit: Option<f64>,
    /// Number of templates to skip
    pub offset: Option<f64>,
    /// Search by template name
    pub search: Option<String>,
    /// Field to sort by
    /// Allowed values: `updatedAt`.
    pub sort_by: Option<String>,
    /// Sort direction
    /// Allowed values: `asc`, `desc`.
    pub sort_order: Option<String>,
    /// Return archived templates
    pub archived: Option<bool>,
    /// Folder to list templates from. Use 'root' for top-level listing.
    pub folder_id: Option<String>,
    /// Whether to include templates, folders, or both in the response. `templates` will
    /// return only templates, `folders` will return only folders, and `all` will return
    /// both.
    /// Allowed values: `all`, `templates`, `folders`.
    pub include: Option<String>,
}

impl ListTemplatesParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Number of templates to return
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// Number of templates to skip
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    /// Search by template name
    pub fn search(mut self, v: impl Into<String>) -> Self {
        self.search = Some(v.into());
        self
    }

    /// Field to sort by
    pub fn sort_by(mut self, v: impl Into<String>) -> Self {
        self.sort_by = Some(v.into());
        self
    }

    /// Sort direction
    pub fn sort_order(mut self, v: impl Into<String>) -> Self {
        self.sort_order = Some(v.into());
        self
    }

    /// Return archived templates
    pub fn archived(mut self, v: bool) -> Self {
        self.archived = Some(v);
        self
    }

    /// Folder to list templates from. Use 'root' for top-level listing.
    pub fn folder_id(mut self, v: impl Into<String>) -> Self {
        self.folder_id = Some(v.into());
        self
    }

    /// Whether to include templates, folders, or both in the response. `templates` will
    /// return only templates, `folders` will return only folders, and `all` will return
    /// both.
    pub fn include(mut self, v: impl Into<String>) -> Self {
        self.include = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        if let Some(v) = &self.search {
            q.push(("search".into(), v.to_string()));
        }
        if let Some(v) = &self.sort_by {
            q.push(("sortBy".into(), v.to_string()));
        }
        if let Some(v) = &self.sort_order {
            q.push(("sortOrder".into(), v.to_string()));
        }
        if let Some(v) = &self.archived {
            q.push(("archived".into(), v.to_string()));
        }
        if let Some(v) = &self.folder_id {
            q.push(("folderId".into(), v.to_string()));
        }
        if let Some(v) = &self.include {
            q.push(("include".into(), v.to_string()));
        }
        q
    }
}

impl EmailsService {
    /// List Bulk Action Campaigns
    ///
    /// Get list of bulk action campaigns for a location
    ///
    /// `GET /emails/locations/{locationId}/campaigns/bulk-actions`
    ///
    /// Requires scope: `emails/campaigns.readonly`.
    pub async fn list_bulk_action_campaigns(
        &self,
        location_id: &str,
        params: &ListBulkActionCampaignsParams,
    ) -> Result<models::ListBulkActionCampaignsPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/bulk-actions",
            crate::services::encode(location_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Get Bulk Action Campaign by ID
    ///
    /// Get a single bulk action campaign by its ID
    ///
    /// `GET /emails/locations/{locationId}/campaigns/bulk-actions/{campaignId}`
    ///
    /// Requires scope: `emails/campaigns.readonly`.
    pub async fn get_bulk_action_campaign_by_id(
        &self,
        location_id: &str,
        campaign_id: &str,
    ) -> Result<models::GetBulkActionCampaignPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/bulk-actions/{}",
            crate::services::encode(location_id),
            crate::services::encode(campaign_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// List Email Campaigns
    ///
    /// Get list of email campaigns for a location
    ///
    /// `GET /emails/locations/{locationId}/campaigns/emails`
    ///
    /// Requires scope: `emails/campaigns.readonly`.
    pub async fn list_email_campaigns(
        &self,
        location_id: &str,
        params: &ListEmailCampaignsParams,
    ) -> Result<models::ListEmailCampaignsPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/emails",
            crate::services::encode(location_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Create Email Campaign
    ///
    /// Create a new email campaign
    ///
    /// `POST /emails/locations/{locationId}/campaigns/emails`
    ///
    /// Requires scope: `emails/campaigns.write`.
    pub async fn create_email_campaign(
        &self,
        location_id: &str,
        body: &models::CreateEmailCampaignPublicV2BodyDto,
    ) -> Result<models::CreateEmailCampaignPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/emails",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Delete Campaign
    ///
    /// Delete a campaign
    ///
    /// `DELETE /emails/locations/{locationId}/campaigns/emails/{campaignId}`
    ///
    /// Requires scope: `emails/campaigns.write`.
    pub async fn delete_campaign(
        &self,
        location_id: &str,
        campaign_id: &str,
    ) -> Result<models::DeleteCampaignPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/emails/{}",
            crate::services::encode(location_id),
            crate::services::encode(campaign_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get Email Campaign by ID
    ///
    /// Get a single email campaign by its ID
    ///
    /// `GET /emails/locations/{locationId}/campaigns/emails/{campaignId}`
    ///
    /// Requires scope: `emails/campaigns.readonly`.
    pub async fn get_email_campaign_by_id(
        &self,
        location_id: &str,
        campaign_id: &str,
    ) -> Result<models::GetEmailCampaignPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/emails/{}",
            crate::services::encode(location_id),
            crate::services::encode(campaign_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Update Email Campaign
    ///
    /// Update an email campaign draft
    ///
    /// `PATCH /emails/locations/{locationId}/campaigns/emails/{campaignId}`
    ///
    /// Requires scope: `emails/campaigns.write`.
    pub async fn update_email_campaign(
        &self,
        location_id: &str,
        campaign_id: &str,
        body: &models::UpdateEmailCampaignPublicV2BodyDto,
    ) -> Result<models::UpdateEmailCampaignPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/emails/{}",
            crate::services::encode(location_id),
            crate::services::encode(campaign_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PATCH,
                &path,
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }

    /// Schedule Campaign
    ///
    /// Schedule or start an email campaign. The campaign must be in draft, cancelled, or
    /// paused status.
    ///
    /// `POST /emails/locations/{locationId}/campaigns/emails/{campaignId}/schedule`
    ///
    /// Requires scope: `emails/campaigns.write`.
    pub async fn schedule_campaign(
        &self,
        location_id: &str,
        campaign_id: &str,
        body: &models::ScheduleCampaignPublicV2BodyDto,
    ) -> Result<models::ScheduleCampaignPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/emails/{}/schedule",
            crate::services::encode(location_id),
            crate::services::encode(campaign_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Get Campaign Statistics
    ///
    /// Get statistics for email campaigns, workflows, or bulk actions
    ///
    /// `GET /emails/locations/{locationId}/campaigns/stats/{source}/{sourceId}`
    ///
    /// Requires scope: `emails/stats.readonly`.
    pub async fn get_campaign_statistics(
        &self,
        location_id: &str,
        source: &str,
        source_id: &str,
        params: &GetCampaignStatisticsParams,
    ) -> Result<models::GetCampaignStatsPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/stats/{}/{}",
            crate::services::encode(location_id),
            crate::services::encode(source),
            crate::services::encode(source_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// List Workflow Campaigns
    ///
    /// Get list of workflow campaigns for a location
    ///
    /// `GET /emails/locations/{locationId}/campaigns/workflows`
    ///
    /// Requires scope: `emails/campaigns.readonly`.
    pub async fn list_workflow_campaigns(
        &self,
        location_id: &str,
        params: &ListWorkflowCampaignsParams,
    ) -> Result<models::ListWorkflowCampaignsPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/workflows",
            crate::services::encode(location_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Get Workflow Campaign by ID
    ///
    /// Get a single workflow campaign by its ID
    ///
    /// `GET /emails/locations/{locationId}/campaigns/workflows/{campaignId}`
    ///
    /// Requires scope: `emails/campaigns.readonly`.
    pub async fn get_workflow_campaign_by_id(
        &self,
        location_id: &str,
        campaign_id: &str,
    ) -> Result<models::GetWorkflowCampaignPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/campaigns/workflows/{}",
            crate::services::encode(location_id),
            crate::services::encode(campaign_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// List templates
    ///
    /// Get list of templates by location
    ///
    /// `GET /emails/locations/{locationId}/templates`
    ///
    /// Requires scope: `emails/templates.readonly`.
    pub async fn list_templates(
        &self,
        location_id: &str,
        params: &ListTemplatesParams,
    ) -> Result<models::ListTemplatesPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/templates",
            crate::services::encode(location_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Create an email template
    ///
    /// Create a new email template
    ///
    /// `POST /emails/locations/{locationId}/templates`
    ///
    /// Requires scope: `emails/templates.write`.
    pub async fn create_an_email_template(
        &self,
        location_id: &str,
        body: &models::CreateTemplatePublicV2BodyDto,
    ) -> Result<models::CreateTemplatePublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/templates",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Create a template folder
    ///
    /// Create a new template folder
    ///
    /// `POST /emails/locations/{locationId}/templates/folders`
    ///
    /// Requires scope: `emails/templates.write`.
    pub async fn create_a_template_folder(
        &self,
        location_id: &str,
        body: &models::CreateTemplateFolderPublicV2BodyDto,
    ) -> Result<models::CreateTemplateFolderPublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/templates/folders",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Import an email template
    ///
    /// Import a template from a provider URL
    ///
    /// `POST /emails/locations/{locationId}/templates/import`
    ///
    /// Requires scope: `emails/templates.write`.
    pub async fn import_an_email_template(
        &self,
        location_id: &str,
        body: &models::ImportTemplatePublicV2BodyDto,
    ) -> Result<models::ImportTemplatePublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/templates/import",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), Some("v3"))
            .await
    }

    /// Delete a template
    ///
    /// `DELETE /emails/locations/{locationId}/templates/{templateId}`
    ///
    /// Requires scope: `emails/templates.write`.
    pub async fn delete_a_template(
        &self,
        location_id: &str,
        template_id: &str,
    ) -> Result<models::DeleteTemplatePublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/templates/{}",
            crate::services::encode(location_id),
            crate::services::encode(template_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("v3"),
            )
            .await
    }

    /// Get Email Template by ID
    ///
    /// Get a single email template by its ID
    ///
    /// `GET /emails/locations/{locationId}/templates/{templateId}`
    ///
    /// Requires scope: `emails/templates.readonly`.
    pub async fn get_email_template_by_id(
        &self,
        location_id: &str,
        template_id: &str,
    ) -> Result<models::GetTemplatePublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/templates/{}",
            crate::services::encode(location_id),
            crate::services::encode(template_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, Some("v3"))
            .await
    }

    /// Update an email template
    ///
    /// Update email template
    ///
    /// `PATCH /emails/locations/{locationId}/templates/{templateId}`
    ///
    /// Requires scope: `emails/templates.write`.
    pub async fn update_an_email_template(
        &self,
        location_id: &str,
        template_id: &str,
        body: &models::UpdateTemplatePublicV2BodyDto,
    ) -> Result<models::UpdateTemplatePublicV2ResponseDto> {
        let path = format!(
            "/emails/locations/{}/templates/{}",
            crate::services::encode(location_id),
            crate::services::encode(template_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PATCH,
                &path,
                &query,
                Some(body),
                Some("v3"),
            )
            .await
    }
}
