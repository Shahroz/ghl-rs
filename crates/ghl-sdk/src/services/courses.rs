// @generated by xtask/generate_services.py — do not edit by hand.
//! `courses` — typed methods for all 1 API v2 operations
//! in this module.
//!
//! Access via [`Ghl::courses`](crate::Ghl::courses).
//!
//! Request and response types come from [`ghl_models::v2::courses`](https://docs.rs/ghl-models/latest/ghl_models/v2/courses/); every endpoint is also documented in the
//! [`courses` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/courses.md).
//!
//! Enable with `features = ["courses"]`.

#![allow(clippy::too_many_arguments)]

use crate::client::Ghl;
use crate::error::Result;
use ghl_models::v2::courses as models;

/// Typed access to the `courses` API v2 surface (1 operations). Obtained via
/// [`Ghl::courses`](crate::Ghl::courses).
#[derive(Debug, Clone)]
pub struct CoursesService {
    pub(crate) client: Ghl,
}

impl CoursesService {
    pub(crate) fn new(client: Ghl) -> Self {
        Self { client }
    }
}

impl CoursesService {
    /// Import Courses
    ///
    /// Import Courses through public channels
    ///
    /// `POST /courses/courses-exporter/public/import`
    pub async fn import_courses(
        &self,
        body: &models::PublicExporterPayload,
    ) -> Result<serde_json::Value> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/courses/courses-exporter/public/import",
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }
}
