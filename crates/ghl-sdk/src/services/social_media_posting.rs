// @generated by xtask/generate_services.py — do not edit by hand.
//! `social-media-posting` — typed methods for all 40 API v2 operations
//! in this module.
//!
//! Access via [`Ghl::social_media_posting`](crate::Ghl::social_media_posting).
//!
//! Request and response types come from [`ghl_models::v2::social_media_posting`](https://docs.rs/ghl-models/latest/ghl_models/v2/social_media_posting/); every endpoint is also documented in the
//! [`social-media-posting` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/social-media-posting.md).
//!
//! Enable with `features = ["social-media-posting"]`.

#![allow(clippy::too_many_arguments)]

use crate::client::Ghl;
use crate::error::Result;
use ghl_models::v2::social_media_posting as models;

/// Typed access to the `social-media-posting` API v2 surface (40 operations). Obtained
/// via [`Ghl::social_media_posting`](crate::Ghl::social_media_posting).
#[derive(Debug, Clone)]
pub struct SocialMediaPostingService {
    pub(crate) client: Ghl,
}

impl SocialMediaPostingService {
    pub(crate) fn new(client: Ghl) -> Self {
        Self { client }
    }
}

/// Query parameters for
/// [`SocialMediaPostingService::starts_o_auth_for_facebook_account`].
#[derive(Debug, Clone, Default)]
pub struct StartsOAuthForFacebookAccountParams {
    /// Account Location Id
    /// Required by the API.
    pub location_id: String,
    /// User ID
    /// Required by the API.
    pub user_id: String,
    /// Facebook Page
    pub page: Option<String>,
    /// Reconnect boolean as string
    pub reconnect: Option<String>,
}

impl StartsOAuthForFacebookAccountParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Facebook Page
    pub fn page(mut self, v: impl Into<String>) -> Self {
        self.page = Some(v.into());
        self
    }

    /// Reconnect boolean as string
    pub fn reconnect(mut self, v: impl Into<String>) -> Self {
        self.reconnect = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("userId".into(), self.user_id.clone()),
        ];
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        if let Some(v) = &self.reconnect {
            q.push(("reconnect".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for
/// [`SocialMediaPostingService::starts_o_auth_for_google_account`].
#[derive(Debug, Clone, Default)]
pub struct StartsOAuthForGoogleAccountParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// User Id
    /// Required by the API.
    pub user_id: String,
    /// Page
    pub page: Option<String>,
    /// Reconnect
    pub reconnect: Option<String>,
}

impl StartsOAuthForGoogleAccountParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Page
    pub fn page(mut self, v: impl Into<String>) -> Self {
        self.page = Some(v.into());
        self
    }

    /// Reconnect
    pub fn reconnect(mut self, v: impl Into<String>) -> Self {
        self.reconnect = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("userId".into(), self.user_id.clone()),
        ];
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        if let Some(v) = &self.reconnect {
            q.push(("reconnect".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for
/// [`SocialMediaPostingService::starts_o_auth_for_instagram_account`].
#[derive(Debug, Clone, Default)]
pub struct StartsOAuthForInstagramAccountParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// User Id
    /// Required by the API.
    pub user_id: String,
    /// Page
    pub page: Option<String>,
    /// Reconnect
    pub reconnect: Option<String>,
}

impl StartsOAuthForInstagramAccountParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Page
    pub fn page(mut self, v: impl Into<String>) -> Self {
        self.page = Some(v.into());
        self
    }

    /// Reconnect
    pub fn reconnect(mut self, v: impl Into<String>) -> Self {
        self.reconnect = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("userId".into(), self.user_id.clone()),
        ];
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        if let Some(v) = &self.reconnect {
            q.push(("reconnect".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for
/// [`SocialMediaPostingService::starts_o_auth_for_linked_in_account`].
#[derive(Debug, Clone, Default)]
pub struct StartsOAuthForLinkedInAccountParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// User Id
    /// Required by the API.
    pub user_id: String,
    /// Page
    pub page: Option<String>,
    /// Reconnect
    pub reconnect: Option<String>,
}

impl StartsOAuthForLinkedInAccountParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Page
    pub fn page(mut self, v: impl Into<String>) -> Self {
        self.page = Some(v.into());
        self
    }

    /// Reconnect
    pub fn reconnect(mut self, v: impl Into<String>) -> Self {
        self.reconnect = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("userId".into(), self.user_id.clone()),
        ];
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        if let Some(v) = &self.reconnect {
            q.push(("reconnect".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for
/// [`SocialMediaPostingService::starts_o_auth_for_tiktok_business_account`].
#[derive(Debug, Clone, Default)]
pub struct StartsOAuthForTiktokBusinessAccountParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// User Id
    /// Required by the API.
    pub user_id: String,
    /// Page
    pub page: Option<String>,
    /// Reconnect
    pub reconnect: Option<String>,
}

impl StartsOAuthForTiktokBusinessAccountParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Page
    pub fn page(mut self, v: impl Into<String>) -> Self {
        self.page = Some(v.into());
        self
    }

    /// Reconnect
    pub fn reconnect(mut self, v: impl Into<String>) -> Self {
        self.reconnect = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("userId".into(), self.user_id.clone()),
        ];
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        if let Some(v) = &self.reconnect {
            q.push(("reconnect".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for
/// [`SocialMediaPostingService::starts_o_auth_for_tiktok_account`].
#[derive(Debug, Clone, Default)]
pub struct StartsOAuthForTiktokAccountParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// User Id
    /// Required by the API.
    pub user_id: String,
    /// Page
    pub page: Option<String>,
    /// Reconnect
    pub reconnect: Option<String>,
}

impl StartsOAuthForTiktokAccountParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Page
    pub fn page(mut self, v: impl Into<String>) -> Self {
        self.page = Some(v.into());
        self
    }

    /// Reconnect
    pub fn reconnect(mut self, v: impl Into<String>) -> Self {
        self.reconnect = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("userId".into(), self.user_id.clone()),
        ];
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        if let Some(v) = &self.reconnect {
            q.push(("reconnect".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for
/// [`SocialMediaPostingService::starts_o_auth_for_twitter_account`].
#[derive(Debug, Clone, Default)]
pub struct StartsOAuthForTwitterAccountParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// User Id
    /// Required by the API.
    pub user_id: String,
    /// Page
    pub page: Option<String>,
    /// Reconnect
    pub reconnect: Option<String>,
}

impl StartsOAuthForTwitterAccountParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Page
    pub fn page(mut self, v: impl Into<String>) -> Self {
        self.page = Some(v.into());
        self
    }

    /// Reconnect
    pub fn reconnect(mut self, v: impl Into<String>) -> Self {
        self.reconnect = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("userId".into(), self.user_id.clone()),
        ];
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        if let Some(v) = &self.reconnect {
            q.push(("reconnect".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`SocialMediaPostingService::get_social_media_statistics`].
#[derive(Debug, Clone, Default)]
pub struct GetSocialMediaStatisticsParams {
    /// Location ID
    /// Required by the API.
    pub location_id: String,
}

impl GetSocialMediaStatisticsParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        q
    }
}

/// Query parameters for [`SocialMediaPostingService::delete_account`].
#[derive(Debug, Clone, Default)]
pub struct DeleteAccountParams {
    /// Company ID
    pub company_id: Option<String>,
    /// User ID
    pub user_id: Option<String>,
}

impl DeleteAccountParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Company ID
    pub fn company_id(mut self, v: impl Into<String>) -> Self {
        self.company_id = Some(v.into());
        self
    }

    /// User ID
    pub fn user_id(mut self, v: impl Into<String>) -> Self {
        self.user_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.company_id {
            q.push(("companyId".into(), v.to_string()));
        }
        if let Some(v) = &self.user_id {
            q.push(("userId".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`SocialMediaPostingService::get_categories_by_location_id`].
#[derive(Debug, Clone, Default)]
pub struct GetCategoriesByLocationIdParams {
    /// Search text string
    pub search_text: Option<String>,
    /// Limit
    pub limit: Option<String>,
    /// Skip
    pub skip: Option<String>,
}

impl GetCategoriesByLocationIdParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Search text string
    pub fn search_text(mut self, v: impl Into<String>) -> Self {
        self.search_text = Some(v.into());
        self
    }

    /// Limit
    pub fn limit(mut self, v: impl Into<String>) -> Self {
        self.limit = Some(v.into());
        self
    }

    /// Skip
    pub fn skip(mut self, v: impl Into<String>) -> Self {
        self.skip = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.search_text {
            q.push(("searchText".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.skip {
            q.push(("skip".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`SocialMediaPostingService::get_upload_status`].
#[derive(Debug, Clone, Default)]
pub struct GetUploadStatusParams {
    /// `skip` query parameter.
    pub skip: Option<String>,
    /// `limit` query parameter.
    pub limit: Option<String>,
    /// `includeUsers` query parameter.
    pub include_users: Option<String>,
    /// User ID
    pub user_id: Option<String>,
}

impl GetUploadStatusParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Set the `skip` query parameter.
    pub fn skip(mut self, v: impl Into<String>) -> Self {
        self.skip = Some(v.into());
        self
    }

    /// Set the `limit` query parameter.
    pub fn limit(mut self, v: impl Into<String>) -> Self {
        self.limit = Some(v.into());
        self
    }

    /// Set the `includeUsers` query parameter.
    pub fn include_users(mut self, v: impl Into<String>) -> Self {
        self.include_users = Some(v.into());
        self
    }

    /// User ID
    pub fn user_id(mut self, v: impl Into<String>) -> Self {
        self.user_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.skip {
            q.push(("skip".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.include_users {
            q.push(("includeUsers".into(), v.to_string()));
        }
        if let Some(v) = &self.user_id {
            q.push(("userId".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`SocialMediaPostingService::get_csv_post`].
#[derive(Debug, Clone, Default)]
pub struct GetCsvPostParams {
    /// `skip` query parameter.
    pub skip: Option<String>,
    /// `limit` query parameter.
    pub limit: Option<String>,
}

impl GetCsvPostParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Set the `skip` query parameter.
    pub fn skip(mut self, v: impl Into<String>) -> Self {
        self.skip = Some(v.into());
        self
    }

    /// Set the `limit` query parameter.
    pub fn limit(mut self, v: impl Into<String>) -> Self {
        self.limit = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.skip {
            q.push(("skip".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`SocialMediaPostingService::get_tags_by_location_id`].
#[derive(Debug, Clone, Default)]
pub struct GetTagsByLocationIdParams {
    /// Search text string
    pub search_text: Option<String>,
    /// Limit
    pub limit: Option<String>,
    /// Skip
    pub skip: Option<String>,
}

impl GetTagsByLocationIdParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Search text string
    pub fn search_text(mut self, v: impl Into<String>) -> Self {
        self.search_text = Some(v.into());
        self
    }

    /// Limit
    pub fn limit(mut self, v: impl Into<String>) -> Self {
        self.limit = Some(v.into());
        self
    }

    /// Skip
    pub fn skip(mut self, v: impl Into<String>) -> Self {
        self.skip = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.search_text {
            q.push(("searchText".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.skip {
            q.push(("skip".into(), v.to_string()));
        }
        q
    }
}

impl SocialMediaPostingService {
    /// Starts OAuth For Facebook Account
    ///
    /// Open the API in a window with appropriate params and headers instead of using the
    /// Curl. User is navigated to Facebook login OAuth screen. On successful login, listen
    /// on window object for message where event listener returns data in its callback
    /// function. ### Sample code to listen to event data:
    /// window.addEventListener('message', function(e) { if (e.data && e.data.page ===
    /// 'social_media_posting') { const { actionType, page, platform, placement, accountId,
    /// reconnectAccounts } = e.data } }, false) ### Event Data Response: { actionType:
    /// string, Ex: "close" page: string, Ex: "social-media-posting"
    ///
    /// `GET /social-media-posting/oauth/facebook/start`
    pub async fn starts_o_auth_for_facebook_account(
        &self,
        params: &StartsOAuthForFacebookAccountParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/social-media-posting/oauth/facebook/start",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Starts OAuth For Google Account
    ///
    /// Open the API in a window with appropriate params and headers instead of using the
    /// Curl. User is navigated to Google login OAuth screen. On successful login, listen on
    /// window object for message where event listener returns data in its callback
    /// function. ### Sample code to listen to event data:
    /// window.addEventListener('message', function(e) { if (e.data && e.data.page ===
    /// 'social_media_posting') { const { actionType, page, platform, placement, accountId,
    /// reconnectAccounts } = e.data } }, false) ### Event Data Response: { actionType:
    /// string, Ex: "close" page: string, Ex: "social-media-posting" pl
    ///
    /// `GET /social-media-posting/oauth/google/start`
    ///
    /// Requires scope: `socialplanner/oauth.readonly`.
    pub async fn starts_o_auth_for_google_account(
        &self,
        params: &StartsOAuthForGoogleAccountParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/social-media-posting/oauth/google/start",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Starts OAuth For Instagram Account
    ///
    /// Open the API in a window with appropriate params and headers instead of using the
    /// Curl. User is navigated to Instagram login OAuth screen. On successful login, listen
    /// on window object for message where event listener returns data in its callback
    /// function. ### Sample code to listen to event data:
    /// window.addEventListener('message', function(e) { if (e.data && e.data.page ===
    /// 'social_media_posting') { const { actionType, page, platform, placement, accountId,
    /// reconnectAccounts } = e.data } }, false) ### Event Data Response: { actionType:
    /// string, Ex: "close" page: string, Ex: "social-media-posting"
    ///
    /// `GET /social-media-posting/oauth/instagram/start`
    ///
    /// Requires scope: `socialplanner/oauth.readonly`.
    pub async fn starts_o_auth_for_instagram_account(
        &self,
        params: &StartsOAuthForInstagramAccountParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/social-media-posting/oauth/instagram/start",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Starts OAuth For LinkedIn Account
    ///
    /// Open the API in a window with appropriate params and headers instead of using the
    /// Curl. User is navigated to LinkedIn login OAuth screen. On successful login, listen
    /// on window object for message where event listener returns data in its callback
    /// function. ### Sample code to listen to event data:
    /// window.addEventListener('message', function(e) { if (e.data && e.data.page ===
    /// 'social_media_posting') { const { actionType, page, platform, placement, accountId,
    /// reconnectAccounts } = e.data } }, false) ### Event Data Response: { actionType:
    /// string, Ex: "close" page: string, Ex: "social-media-posting"
    ///
    /// `GET /social-media-posting/oauth/linkedin/start`
    pub async fn starts_o_auth_for_linked_in_account(
        &self,
        params: &StartsOAuthForLinkedInAccountParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/social-media-posting/oauth/linkedin/start",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Starts OAuth For Tiktok Business Account
    ///
    /// Open the API in a window with appropriate params and headers instead of using the
    /// Curl. User is navigated to Tiktok-Business login OAuth screen. On successful login,
    /// listen on window object for message where event listener returns data in its
    /// callback function. ### Sample code to listen to event data:
    /// window.addEventListener('message', function(e) { if (e.data && e.data.page ===
    /// 'social_media_posting') { const { actionType, page, platform, placement, accountId,
    /// reconnectAccounts } = e.data } }, false) ### Event Data Response: { actionType:
    /// string, Ex: "close" page: string, Ex: "social-media-po
    ///
    /// `GET /social-media-posting/oauth/tiktok-business/start`
    ///
    /// Requires scope: `socialplanner/oauth.readonly`.
    pub async fn starts_o_auth_for_tiktok_business_account(
        &self,
        params: &StartsOAuthForTiktokBusinessAccountParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/social-media-posting/oauth/tiktok-business/start",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Starts OAuth For Tiktok Account
    ///
    /// Open the API in a window with appropriate params and headers instead of using the
    /// Curl. User is navigated to Tiktok login OAuth screen. On successful login, listen on
    /// window object for message where event listener returns data in its callback
    /// function. ### Sample code to listen to event data:
    /// window.addEventListener('message', function(e) { if (e.data && e.data.page ===
    /// 'social_media_posting') { const { actionType, page, platform, placement, accountId,
    /// reconnectAccounts } = e.data } }, false) ### Event Data Response: { actionType:
    /// string, Ex: "close" page: string, Ex: "social-media-posting" pl
    ///
    /// `GET /social-media-posting/oauth/tiktok/start`
    ///
    /// Requires scope: `socialplanner/oauth.readonly`.
    pub async fn starts_o_auth_for_tiktok_account(
        &self,
        params: &StartsOAuthForTiktokAccountParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/social-media-posting/oauth/tiktok/start",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Starts OAuth For Twitter Account
    ///
    /// ! As of December 4, 2024, X (formerly Twitter) is no longer supported. We apologise
    /// for any inconvenience.
    ///
    /// `GET /social-media-posting/oauth/twitter/start`
    ///
    /// Requires scope: `socialplanner/oauth.readonly`.
    pub async fn starts_o_auth_for_twitter_account(
        &self,
        params: &StartsOAuthForTwitterAccountParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/social-media-posting/oauth/twitter/start",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get facebook pages
    ///
    /// `GET /social-media-posting/oauth/{locationId}/facebook/accounts/{accountId}`
    pub async fn get_facebook_pages(
        &self,
        location_id: &str,
        account_id: &str,
    ) -> Result<models::GetFacebookAccountsResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/facebook/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Attach facebook pages
    ///
    /// `POST /social-media-posting/oauth/{locationId}/facebook/accounts/{accountId}`
    pub async fn attach_facebook_pages(
        &self,
        location_id: &str,
        account_id: &str,
        body: &models::AttachFBAccountDTO,
    ) -> Result<models::SocialMediaFBAccountResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/facebook/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get google business locations
    ///
    /// `GET /social-media-posting/oauth/{locationId}/google/locations/{accountId}`
    pub async fn get_google_business_locations(
        &self,
        location_id: &str,
        account_id: &str,
    ) -> Result<models::GetGoogleLocationResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/google/locations/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Set google business locations
    ///
    /// `POST /social-media-posting/oauth/{locationId}/google/locations/{accountId}`
    pub async fn set_google_business_locations(
        &self,
        location_id: &str,
        account_id: &str,
        body: &models::AttachGMBLocationDTO,
    ) -> Result<models::SocialMediaGmbAccountResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/google/locations/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Instagram Professional Accounts
    ///
    /// `GET /social-media-posting/oauth/{locationId}/instagram/accounts/{accountId}`
    pub async fn get_instagram_professional_accounts(
        &self,
        location_id: &str,
        account_id: &str,
    ) -> Result<models::GetInstagramAccountsResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/instagram/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Attach Instagram Professional Accounts
    ///
    /// `POST /social-media-posting/oauth/{locationId}/instagram/accounts/{accountId}`
    pub async fn attach_instagram_professional_accounts(
        &self,
        location_id: &str,
        account_id: &str,
        body: &models::AttachIGAccountDTO,
    ) -> Result<models::SocialMediaInstagramAccountResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/instagram/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Linkedin pages and profile
    ///
    /// `GET /social-media-posting/oauth/{locationId}/linkedin/accounts/{accountId}`
    pub async fn get_linkedin_pages_and_profile(
        &self,
        location_id: &str,
        account_id: &str,
    ) -> Result<models::GetLinkedInAccountsResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/linkedin/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Attach linkedin pages and profile
    ///
    /// `POST /social-media-posting/oauth/{locationId}/linkedin/accounts/{accountId}`
    pub async fn attach_linkedin_pages_and_profile(
        &self,
        location_id: &str,
        account_id: &str,
        body: &models::AttachLinkedinAccountDTO,
    ) -> Result<models::SocialMediaLinkedInAccountResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/linkedin/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Tiktok Business profile
    ///
    /// `GET /social-media-posting/oauth/{locationId}/tiktok-business/accounts/{accountId}`
    pub async fn get_tiktok_business_profile(
        &self,
        location_id: &str,
        account_id: &str,
    ) -> Result<models::GetTiktokBusinessAccountResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/tiktok-business/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Tiktok profile
    ///
    /// `GET /social-media-posting/oauth/{locationId}/tiktok/accounts/{accountId}`
    pub async fn get_tiktok_profile(
        &self,
        location_id: &str,
        account_id: &str,
    ) -> Result<models::GetTiktokAccountResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/tiktok/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Attach Tiktok profile
    ///
    /// `POST /social-media-posting/oauth/{locationId}/tiktok/accounts/{accountId}`
    pub async fn attach_tiktok_profile(
        &self,
        location_id: &str,
        account_id: &str,
        body: &models::AttachTiktokAccountDTO,
    ) -> Result<models::SocialMediaTiktokAccountResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/tiktok/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Twitter profile
    ///
    /// ! As of December 4, 2024, X (formerly Twitter) is no longer supported. We apologise
    /// for any inconvenience.
    ///
    /// `GET /social-media-posting/oauth/{locationId}/twitter/accounts/{accountId}`
    pub async fn get_twitter_profile(
        &self,
        location_id: &str,
        account_id: &str,
    ) -> Result<models::GetTwitterAccountsResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/twitter/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Attach Twitter profile
    ///
    /// ! As of December 4, 2024, X (formerly Twitter) is no longer supported. We apologise
    /// for any inconvenience.
    ///
    /// `POST /social-media-posting/oauth/{locationId}/twitter/accounts/{accountId}`
    pub async fn attach_twitter_profile(
        &self,
        location_id: &str,
        account_id: &str,
        body: &models::AttachTwitterAccountDTO,
    ) -> Result<models::SocialMediaTwitterAccountResponseDTO> {
        let path = format!(
            "/social-media-posting/oauth/{}/twitter/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(account_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Social Media Statistics
    ///
    /// Retrieve analytics data for multiple social media accounts. Provides metrics for the
    /// last 7 days with comparison to the previous 7 days. Supports filtering by platforms
    /// and specific connected accounts.
    ///
    /// `POST /social-media-posting/statistics`
    pub async fn get_social_media_statistics(
        &self,
        params: &GetSocialMediaStatisticsParams,
        body: &serde_json::Value,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/social-media-posting/statistics",
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Accounts
    ///
    /// Get list of accounts and groups
    ///
    /// `GET /social-media-posting/{locationId}/accounts`
    ///
    /// Requires scope: `socialplanner/account.readonly`.
    pub async fn get_accounts(&self, location_id: &str) -> Result<models::AccountsListResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/accounts",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Delete Account
    ///
    /// Delete account and account from group
    ///
    /// `DELETE /social-media-posting/{locationId}/accounts/{id}`
    pub async fn delete_account(
        &self,
        location_id: &str,
        id: &str,
        params: &DeleteAccountParams,
    ) -> Result<models::LocationAndAccountDeleteResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/accounts/{}",
            crate::services::encode(location_id),
            crate::services::encode(id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get categories by location id
    ///
    /// `GET /social-media-posting/{locationId}/categories`
    ///
    /// Requires scope: `socialplanner/category.readonly`.
    pub async fn get_categories_by_location_id(
        &self,
        location_id: &str,
        params: &GetCategoriesByLocationIdParams,
    ) -> Result<models::GetByLocationIdResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/categories",
            crate::services::encode(location_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get categories by id
    ///
    /// `GET /social-media-posting/{locationId}/categories/{id}`
    pub async fn get_categories_by_id(
        &self,
        location_id: &str,
        id: &str,
    ) -> Result<models::GetByIdResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/categories/{}",
            crate::services::encode(location_id),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Upload Status
    ///
    /// `GET /social-media-posting/{locationId}/csv`
    pub async fn get_upload_status(
        &self,
        location_id: &str,
        params: &GetUploadStatusParams,
    ) -> Result<models::GetUploadStatusResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/csv",
            crate::services::encode(location_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Upload CSV
    ///
    /// `POST /social-media-posting/{locationId}/csv`
    ///
    /// Requires scope: `socialplanner/csv.write`.
    pub async fn upload_csv(
        &self,
        location_id: &str,
        body: &serde_json::Value,
    ) -> Result<models::UploadFileResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/csv",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Delete CSV Post
    ///
    /// `DELETE /social-media-posting/{locationId}/csv/{csvId}/post/{postId}`
    pub async fn delete_csv_post(
        &self,
        location_id: &str,
        csv_id: &str,
        post_id: &str,
    ) -> Result<models::DeletePostResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/csv/{}/post/{}",
            crate::services::encode(location_id),
            crate::services::encode(csv_id),
            crate::services::encode(post_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Delete CSV
    ///
    /// `DELETE /social-media-posting/{locationId}/csv/{id}`
    pub async fn delete_csv(
        &self,
        location_id: &str,
        id: &str,
    ) -> Result<models::DeleteCsvResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/csv/{}",
            crate::services::encode(location_id),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get CSV Post
    ///
    /// `GET /social-media-posting/{locationId}/csv/{id}`
    pub async fn get_csv_post(
        &self,
        location_id: &str,
        id: &str,
        params: &GetCsvPostParams,
    ) -> Result<models::GetCsvPostResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/csv/{}",
            crate::services::encode(location_id),
            crate::services::encode(id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Start CSV Finalize
    ///
    /// `PATCH /social-media-posting/{locationId}/csv/{id}`
    pub async fn start_csv_finalize(
        &self,
        location_id: &str,
        id: &str,
        body: &models::CSVDefaultDTO,
    ) -> Result<models::CsvPostStatusResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/csv/{}",
            crate::services::encode(location_id),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PATCH,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Create post
    ///
    /// Create posts for all supported platforms. It is possible to create customized posts
    /// per channel by using the same platform account IDs in a request and hitting the
    /// create post API multiple times with different summaries and account IDs per
    /// platform. The content and media limitations, as well as platform rate limiters
    /// corresponding to the respective platforms, are provided in the following reference
    /// link: Link: [Platform
    /// Limitations](https://help.leadconnectorhq.com/support/solutions/articles/48001240003-social-planner-image-video-content-and-api-limitations
    /// "Social Planner Help")
    ///
    /// `POST /social-media-posting/{locationId}/posts`
    ///
    /// Requires scope: `socialplanner/post.write`.
    pub async fn create_post(
        &self,
        location_id: &str,
        body: &models::CreatePostDTO,
    ) -> Result<models::CreatePostSuccessfulResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/posts",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Bulk Delete Social Planner Posts
    ///
    /// Deletes multiple posts based on the provided list of post IDs. This operation is
    /// useful for clearing up large numbers of posts efficiently. Note: 1.The maximum
    /// number of posts that can be deleted in a single request is '50'. 2.However, It will
    /// only get deleted in Highlevel database but still it is recommended to be cautious of
    /// this operation.
    ///
    /// `POST /social-media-posting/{locationId}/posts/bulk-delete`
    pub async fn bulk_delete_social_planner_posts(
        &self,
        location_id: &str,
        body: &models::DeletePostsDto,
    ) -> Result<models::BulkDeleteResponseDto> {
        let path = format!(
            "/social-media-posting/{}/posts/bulk-delete",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get posts
    ///
    /// Get Posts
    ///
    /// `POST /social-media-posting/{locationId}/posts/list`
    ///
    /// Requires scope: `socialplanner/post.readonly`.
    pub async fn get_posts(
        &self,
        location_id: &str,
        body: &models::SearchPostDTO,
    ) -> Result<models::PostSuccessfulResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/posts/list",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Delete Post
    ///
    /// `DELETE /social-media-posting/{locationId}/posts/{id}`
    pub async fn delete_post(
        &self,
        location_id: &str,
        id: &str,
    ) -> Result<models::DeletePostSuccessfulResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/posts/{}",
            crate::services::encode(location_id),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get post
    ///
    /// `GET /social-media-posting/{locationId}/posts/{id}`
    pub async fn get_post(
        &self,
        location_id: &str,
        id: &str,
    ) -> Result<models::GetPostSuccessfulResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/posts/{}",
            crate::services::encode(location_id),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Edit post
    ///
    /// Create posts for all supported platforms. It is possible to create customized posts
    /// per channel by using the same platform account IDs in a request and hitting the
    /// create post API multiple times with different summaries and account IDs per
    /// platform. The content and media limitations, as well as platform rate limiters
    /// corresponding to the respective platforms, are provided in the following reference
    /// link: Link: [Platform
    /// Limitations](https://help.leadconnectorhq.com/support/solutions/articles/48001240003-social-planner-image-video-content-and-api-limitations
    /// "Social Planner Help")
    ///
    /// `PUT /social-media-posting/{locationId}/posts/{id}`
    pub async fn edit_post(
        &self,
        location_id: &str,
        id: &str,
        body: &models::PostCreateRequest,
    ) -> Result<models::UpdatePostSuccessfulResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/posts/{}",
            crate::services::encode(location_id),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Set Accounts
    ///
    /// `POST /social-media-posting/{locationId}/set-accounts`
    ///
    /// Requires scope: `socialplanner/csv.write`.
    pub async fn set_accounts(
        &self,
        location_id: &str,
        body: &models::SetAccountsDTO,
    ) -> Result<models::SetAccountsResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/set-accounts",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get tags by location id
    ///
    /// `GET /social-media-posting/{locationId}/tags`
    pub async fn get_tags_by_location_id(
        &self,
        location_id: &str,
        params: &GetTagsByLocationIdParams,
    ) -> Result<models::GetTagsByLocationIdResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/tags",
            crate::services::encode(location_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get tags by ids
    ///
    /// `POST /social-media-posting/{locationId}/tags/details`
    pub async fn get_tags_by_ids(
        &self,
        location_id: &str,
        body: &models::UpdateTagDTO,
    ) -> Result<models::GetTagsByIdResponseDTO> {
        let path = format!(
            "/social-media-posting/{}/tags/details",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }
}
