// @generated by xtask/generate_services.py — do not edit by hand.
//! `opportunities` — typed methods for all 12 API v2 operations
//! in this module.
//!
//! These methods live on the same [`OpportunitiesService`](crate::opportunities::OpportunitiesService) you get from [`Ghl::opportunities`](crate::Ghl::opportunities), alongside the hand-written helpers.
//!
//! Request and response types come from [`ghl_models::v2::opportunities`](https://docs.rs/ghl-models/latest/ghl_models/v2/opportunities/); every endpoint is also documented in the
//! [`opportunities` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/opportunities.md).
//!
//! Enable with `features = ["opportunities"]`.

#![allow(clippy::too_many_arguments)]

use crate::error::Result;
use ghl_models::v2::opportunities as models;

use crate::opportunities::OpportunitiesService;

/// Query parameters for [`OpportunitiesService::get_lost_reason`].
#[derive(Debug, Clone, Default)]
pub struct GetLostReasonParams {
    /// `locationId` query parameter.
    /// Required by the API.
    pub location_id: String,
    /// lost reason name
    pub name: Option<String>,
    /// deleted
    pub deleted: Option<bool>,
    /// search query
    pub query: Option<String>,
    /// skip
    pub skip: Option<f64>,
    /// limit
    pub limit: Option<f64>,
    /// get count
    pub get_count: Option<bool>,
}

impl GetLostReasonParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            ..Default::default()
        }
    }

    /// lost reason name
    pub fn name(mut self, v: impl Into<String>) -> Self {
        self.name = Some(v.into());
        self
    }

    /// deleted
    pub fn deleted(mut self, v: bool) -> Self {
        self.deleted = Some(v);
        self
    }

    /// search query
    pub fn query(mut self, v: impl Into<String>) -> Self {
        self.query = Some(v.into());
        self
    }

    /// skip
    pub fn skip(mut self, v: f64) -> Self {
        self.skip = Some(v);
        self
    }

    /// limit
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// get count
    pub fn get_count(mut self, v: bool) -> Self {
        self.get_count = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        if let Some(v) = &self.name {
            q.push(("name".into(), v.to_string()));
        }
        if let Some(v) = &self.deleted {
            q.push(("deleted".into(), v.to_string()));
        }
        if let Some(v) = &self.query {
            q.push(("query".into(), v.to_string()));
        }
        if let Some(v) = &self.skip {
            q.push(("skip".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.get_count {
            q.push(("getCount".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`OpportunitiesService::get_pipelines`].
#[derive(Debug, Clone, Default)]
pub struct GetPipelinesParams {
    /// `locationId` query parameter.
    /// Required by the API.
    pub location_id: String,
}

impl GetPipelinesParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        q
    }
}

/// Query parameters for [`OpportunitiesService::search_opportunity`].
#[derive(Debug, Clone, Default)]
pub struct SearchOpportunityParams {
    /// `q` query parameter.
    pub q: Option<String>,
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// Pipeline Id
    pub pipeline_id: Option<String>,
    /// stage Id
    pub pipeline_stage_id: Option<String>,
    /// Contact Id
    pub contact_id: Option<String>,
    /// `status` query parameter.
    /// Allowed values: `open`, `won`, `lost`, `abandoned`, `all`.
    pub status: Option<String>,
    /// `assigned_to` query parameter.
    pub assigned_to: Option<String>,
    /// Campaign Id
    pub campaign_id: Option<String>,
    /// Opportunity Id
    pub id: Option<String>,
    /// `order` query parameter.
    pub order: Option<String>,
    /// End date
    pub end_date: Option<String>,
    /// Start After
    pub start_after: Option<String>,
    /// Start After Id
    pub start_after_id: Option<String>,
    /// Start date
    pub date: Option<String>,
    /// `country` query parameter.
    pub country: Option<String>,
    /// `page` query parameter.
    pub page: Option<f64>,
    /// Limit Per Page records count. will allow maximum up to 100 and default will be 20
    pub limit: Option<f64>,
    /// get Tasks in contact
    pub get_tasks: Option<bool>,
    /// get Notes in contact
    pub get_notes: Option<bool>,
    /// get Calender event in contact
    pub get_calendar_events: Option<bool>,
}

impl SearchOpportunityParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            ..Default::default()
        }
    }

    /// Set the `q` query parameter.
    pub fn q(mut self, v: impl Into<String>) -> Self {
        self.q = Some(v.into());
        self
    }

    /// Pipeline Id
    pub fn pipeline_id(mut self, v: impl Into<String>) -> Self {
        self.pipeline_id = Some(v.into());
        self
    }

    /// stage Id
    pub fn pipeline_stage_id(mut self, v: impl Into<String>) -> Self {
        self.pipeline_stage_id = Some(v.into());
        self
    }

    /// Contact Id
    pub fn contact_id(mut self, v: impl Into<String>) -> Self {
        self.contact_id = Some(v.into());
        self
    }

    /// Set the `status` query parameter.
    pub fn status(mut self, v: impl Into<String>) -> Self {
        self.status = Some(v.into());
        self
    }

    /// Set the `assigned_to` query parameter.
    pub fn assigned_to(mut self, v: impl Into<String>) -> Self {
        self.assigned_to = Some(v.into());
        self
    }

    /// Campaign Id
    pub fn campaign_id(mut self, v: impl Into<String>) -> Self {
        self.campaign_id = Some(v.into());
        self
    }

    /// Opportunity Id
    pub fn id(mut self, v: impl Into<String>) -> Self {
        self.id = Some(v.into());
        self
    }

    /// Set the `order` query parameter.
    pub fn order(mut self, v: impl Into<String>) -> Self {
        self.order = Some(v.into());
        self
    }

    /// End date
    pub fn end_date(mut self, v: impl Into<String>) -> Self {
        self.end_date = Some(v.into());
        self
    }

    /// Start After
    pub fn start_after(mut self, v: impl Into<String>) -> Self {
        self.start_after = Some(v.into());
        self
    }

    /// Start After Id
    pub fn start_after_id(mut self, v: impl Into<String>) -> Self {
        self.start_after_id = Some(v.into());
        self
    }

    /// Start date
    pub fn date(mut self, v: impl Into<String>) -> Self {
        self.date = Some(v.into());
        self
    }

    /// Set the `country` query parameter.
    pub fn country(mut self, v: impl Into<String>) -> Self {
        self.country = Some(v.into());
        self
    }

    /// Set the `page` query parameter.
    pub fn page(mut self, v: f64) -> Self {
        self.page = Some(v);
        self
    }

    /// Limit Per Page records count. will allow maximum up to 100 and default will be 20
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// get Tasks in contact
    pub fn get_tasks(mut self, v: bool) -> Self {
        self.get_tasks = Some(v);
        self
    }

    /// get Notes in contact
    pub fn get_notes(mut self, v: bool) -> Self {
        self.get_notes = Some(v);
        self
    }

    /// get Calender event in contact
    pub fn get_calendar_events(mut self, v: bool) -> Self {
        self.get_calendar_events = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("location_id".into(), self.location_id.clone())];
        if let Some(v) = &self.q {
            q.push(("q".into(), v.to_string()));
        }
        if let Some(v) = &self.pipeline_id {
            q.push(("pipeline_id".into(), v.to_string()));
        }
        if let Some(v) = &self.pipeline_stage_id {
            q.push(("pipeline_stage_id".into(), v.to_string()));
        }
        if let Some(v) = &self.contact_id {
            q.push(("contact_id".into(), v.to_string()));
        }
        if let Some(v) = &self.status {
            q.push(("status".into(), v.to_string()));
        }
        if let Some(v) = &self.assigned_to {
            q.push(("assigned_to".into(), v.to_string()));
        }
        if let Some(v) = &self.campaign_id {
            q.push(("campaignId".into(), v.to_string()));
        }
        if let Some(v) = &self.id {
            q.push(("id".into(), v.to_string()));
        }
        if let Some(v) = &self.order {
            q.push(("order".into(), v.to_string()));
        }
        if let Some(v) = &self.end_date {
            q.push(("endDate".into(), v.to_string()));
        }
        if let Some(v) = &self.start_after {
            q.push(("startAfter".into(), v.to_string()));
        }
        if let Some(v) = &self.start_after_id {
            q.push(("startAfterId".into(), v.to_string()));
        }
        if let Some(v) = &self.date {
            q.push(("date".into(), v.to_string()));
        }
        if let Some(v) = &self.country {
            q.push(("country".into(), v.to_string()));
        }
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.get_tasks {
            q.push(("getTasks".into(), v.to_string()));
        }
        if let Some(v) = &self.get_notes {
            q.push(("getNotes".into(), v.to_string()));
        }
        if let Some(v) = &self.get_calendar_events {
            q.push(("getCalendarEvents".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`OpportunitiesService::remove_followers`].
#[derive(Debug, Clone, Default)]
pub struct RemoveFollowersParams {
    /// `isRemoveAllFollowers` query parameter.
    pub is_remove_all_followers: Option<bool>,
}

impl RemoveFollowersParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Set the `isRemoveAllFollowers` query parameter.
    pub fn is_remove_all_followers(mut self, v: bool) -> Self {
        self.is_remove_all_followers = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.is_remove_all_followers {
            q.push(("isRemoveAllFollowers".into(), v.to_string()));
        }
        q
    }
}

impl OpportunitiesService {
    /// Create Opportunity
    ///
    /// `POST /opportunities/`
    ///
    /// Requires scope: `opportunities.write`.
    pub async fn create_opportunity(
        &self,
        body: &models::CreateDto,
    ) -> Result<models::GetPostOpportunitySuccessfulResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/opportunities/",
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Get lost reason
    ///
    /// `GET /opportunities/lost-reason`
    ///
    /// Requires scope: `opportunities.readonly`.
    pub async fn get_lost_reason(
        &self,
        params: &GetLostReasonParams,
    ) -> Result<models::LostReasonsResponseSchema> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/opportunities/lost-reason",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Pipelines
    ///
    /// `GET /opportunities/pipelines`
    ///
    /// Requires scope: `opportunities.readonly`.
    pub async fn get_pipelines(
        &self,
        params: &GetPipelinesParams,
    ) -> Result<models::GetPipelinesSuccessfulResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/opportunities/pipelines",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Search Opportunity
    ///
    /// `GET /opportunities/search`
    ///
    /// Requires scope: `opportunities.readonly`.
    pub async fn search_opportunity(
        &self,
        params: &SearchOpportunityParams,
    ) -> Result<models::SearchSuccessfulResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/opportunities/search",
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Search Opportunities
    ///
    /// Search Opportunities based on combinations of advanced filters. Documentation Link -
    /// <https://doc.clickup.com/8631005/d/h/87cpx-424216/7bf11bc9b94f80f>
    ///
    /// `POST /opportunities/search`
    ///
    /// Requires scope: `opportunities.readonly`.
    pub async fn search_opportunities(
        &self,
        body: &models::OpportunitySearchBodyDTO,
    ) -> Result<models::PostSearchSuccessfulResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/opportunities/search",
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Upsert Opportunity
    ///
    /// `POST /opportunities/upsert`
    ///
    /// Requires scope: `opportunities.write`.
    pub async fn upsert_opportunity(
        &self,
        body: &models::UpsertOpportunityDto,
    ) -> Result<models::UpsertOpportunitySuccessfulResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/opportunities/upsert",
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Delete Opportunity
    ///
    /// `DELETE /opportunities/{id}`
    ///
    /// Requires scope: `opportunities.write`.
    pub async fn delete_opportunity(
        &self,
        id: &str,
    ) -> Result<models::DeleteUpdateOpportunitySuccessfulResponseDto> {
        let path = format!("/opportunities/{}", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Get Opportunity
    ///
    /// `GET /opportunities/{id}`
    ///
    /// Requires scope: `opportunities.readonly`.
    pub async fn get_opportunity(
        &self,
        id: &str,
    ) -> Result<models::GetPostOpportunitySuccessfulResponseDto> {
        let path = format!("/opportunities/{}", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-07-28"),
            )
            .await
    }

    /// Update Opportunity
    ///
    /// `PUT /opportunities/{id}`
    ///
    /// Requires scope: `opportunities.write`.
    pub async fn update_opportunity(
        &self,
        id: &str,
        body: &models::UpdateOpportunityDto,
    ) -> Result<models::GetPostOpportunitySuccessfulResponseDto> {
        let path = format!("/opportunities/{}", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Remove Followers
    ///
    /// Allows removal of one or all followers from an opportunity.
    ///
    /// `DELETE /opportunities/{id}/followers`
    ///
    /// Requires scope: `opportunities.write`.
    pub async fn remove_followers(
        &self,
        id: &str,
        params: &RemoveFollowersParams,
        body: &models::FollowersDTO,
    ) -> Result<models::DeleteFollowersSuccessfulResponseDto> {
        let path = format!("/opportunities/{}/followers", crate::services::encode(id));
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Add Followers
    ///
    /// `POST /opportunities/{id}/followers`
    ///
    /// Requires scope: `opportunities.write`.
    pub async fn add_followers(
        &self,
        id: &str,
        body: &models::FollowersDTO,
    ) -> Result<models::CreateAddFollowersSuccessfulResponseDto> {
        let path = format!("/opportunities/{}/followers", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }

    /// Update Opportunity Status
    ///
    /// `PUT /opportunities/{id}/status`
    ///
    /// Requires scope: `opportunities.write`.
    pub async fn update_opportunity_status(
        &self,
        id: &str,
        body: &models::UpdateStatusDto,
    ) -> Result<models::DeleteUpdateOpportunitySuccessfulResponseDto> {
        let path = format!("/opportunities/{}/status", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-07-28"),
            )
            .await
    }
}
