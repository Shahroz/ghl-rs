// @generated by xtask/generate_services.py — do not edit by hand.
//! `calendars` — typed methods for all 41 API v2 operations
//! in this module.
//!
//! These methods live on the same [`CalendarsService`](crate::calendars::CalendarsService) you get from [`Ghl::calendars`](crate::Ghl::calendars), alongside the hand-written helpers.
//!
//! Request and response types come from [`ghl_models::v2::calendars`](https://docs.rs/ghl-models/latest/ghl_models/v2/calendars/); every endpoint is also documented in the
//! [`calendars` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/calendars.md).
//!
//! Enable with `features = ["calendars"]`.

#![allow(clippy::too_many_arguments)]

use crate::error::Result;
use ghl_models::v2::calendars as models;

use crate::calendars::CalendarsService;

/// Query parameters for [`CalendarsService::get_calendars`].
#[derive(Debug, Clone, Default)]
pub struct GetCalendarsParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// Group Id
    pub group_id: Option<String>,
    /// Show drafted
    pub show_drafted: Option<bool>,
}

impl GetCalendarsParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            ..Default::default()
        }
    }

    /// Group Id
    pub fn group_id(mut self, v: impl Into<String>) -> Self {
        self.group_id = Some(v.into());
        self
    }

    /// Show drafted
    pub fn show_drafted(mut self, v: bool) -> Self {
        self.show_drafted = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        if let Some(v) = &self.group_id {
            q.push(("groupId".into(), v.to_string()));
        }
        if let Some(v) = &self.show_drafted {
            q.push(("showDrafted".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`CalendarsService::get_notes`].
#[derive(Debug, Clone, Default)]
pub struct GetNotesParams {
    /// Limit of notes to fetch
    /// Required by the API.
    pub limit: f64,
    /// Offset of notes to fetch
    /// Required by the API.
    pub offset: f64,
}

impl GetNotesParams {
    /// Start from the parameters the API requires.
    pub fn new(limit: f64, offset: f64) -> Self {
        Self { limit, offset }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("limit".into(), self.limit.to_string()),
            ("offset".into(), self.offset.to_string()),
        ];
        q
    }
}

/// Query parameters for [`CalendarsService::get_blocked_slots`].
#[derive(Debug, Clone, Default)]
pub struct GetBlockedSlotsParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// User Id - Owner of an appointment. Either of userId, groupId or calendarId is
    /// required
    pub user_id: Option<String>,
    /// Either of calendarId, userId or groupId is required
    pub calendar_id: Option<String>,
    /// Either of groupId, calendarId or userId is required
    pub group_id: Option<String>,
    /// Start Time (in millis)
    /// Required by the API.
    pub start_time: String,
    /// End Time (in millis)
    /// Required by the API.
    pub end_time: String,
}

impl GetBlockedSlotsParams {
    /// Start from the parameters the API requires.
    pub fn new(
        location_id: impl Into<String>,
        start_time: impl Into<String>,
        end_time: impl Into<String>,
    ) -> Self {
        Self {
            location_id: location_id.into(),
            start_time: start_time.into(),
            end_time: end_time.into(),
            ..Default::default()
        }
    }

    /// User Id - Owner of an appointment. Either of userId, groupId or calendarId is
    /// required
    pub fn user_id(mut self, v: impl Into<String>) -> Self {
        self.user_id = Some(v.into());
        self
    }

    /// Either of calendarId, userId or groupId is required
    pub fn calendar_id(mut self, v: impl Into<String>) -> Self {
        self.calendar_id = Some(v.into());
        self
    }

    /// Either of groupId, calendarId or userId is required
    pub fn group_id(mut self, v: impl Into<String>) -> Self {
        self.group_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("startTime".into(), self.start_time.clone()),
            ("endTime".into(), self.end_time.clone()),
        ];
        if let Some(v) = &self.user_id {
            q.push(("userId".into(), v.to_string()));
        }
        if let Some(v) = &self.calendar_id {
            q.push(("calendarId".into(), v.to_string()));
        }
        if let Some(v) = &self.group_id {
            q.push(("groupId".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`CalendarsService::get_calendar_events`].
#[derive(Debug, Clone, Default)]
pub struct GetCalendarEventsParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
    /// User Id - Owner of an appointment. Either of userId, groupId or calendarId is
    /// required
    pub user_id: Option<String>,
    /// Either of calendarId, userId or groupId is required
    pub calendar_id: Option<String>,
    /// Either of groupId, calendarId or userId is required
    pub group_id: Option<String>,
    /// Start Time (in millis)
    /// Required by the API.
    pub start_time: String,
    /// End Time (in millis)
    /// Required by the API.
    pub end_time: String,
}

impl GetCalendarEventsParams {
    /// Start from the parameters the API requires.
    pub fn new(
        location_id: impl Into<String>,
        start_time: impl Into<String>,
        end_time: impl Into<String>,
    ) -> Self {
        Self {
            location_id: location_id.into(),
            start_time: start_time.into(),
            end_time: end_time.into(),
            ..Default::default()
        }
    }

    /// User Id - Owner of an appointment. Either of userId, groupId or calendarId is
    /// required
    pub fn user_id(mut self, v: impl Into<String>) -> Self {
        self.user_id = Some(v.into());
        self
    }

    /// Either of calendarId, userId or groupId is required
    pub fn calendar_id(mut self, v: impl Into<String>) -> Self {
        self.calendar_id = Some(v.into());
        self
    }

    /// Either of groupId, calendarId or userId is required
    pub fn group_id(mut self, v: impl Into<String>) -> Self {
        self.group_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("startTime".into(), self.start_time.clone()),
            ("endTime".into(), self.end_time.clone()),
        ];
        if let Some(v) = &self.user_id {
            q.push(("userId".into(), v.to_string()));
        }
        if let Some(v) = &self.calendar_id {
            q.push(("calendarId".into(), v.to_string()));
        }
        if let Some(v) = &self.group_id {
            q.push(("groupId".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`CalendarsService::get_groups`].
#[derive(Debug, Clone, Default)]
pub struct GetGroupsParams {
    /// Location Id
    /// Required by the API.
    pub location_id: String,
}

impl GetGroupsParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("locationId".into(), self.location_id.clone())];
        q
    }
}

/// Query parameters for [`CalendarsService::list_calendar_resources`].
#[derive(Debug, Clone, Default)]
pub struct ListCalendarResourcesParams {
    /// `locationId` query parameter.
    /// Required by the API.
    pub location_id: String,
    /// `limit` query parameter.
    /// Required by the API.
    pub limit: f64,
    /// `skip` query parameter.
    /// Required by the API.
    pub skip: f64,
}

impl ListCalendarResourcesParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>, limit: f64, skip: f64) -> Self {
        Self {
            location_id: location_id.into(),
            limit,
            skip,
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("limit".into(), self.limit.to_string()),
            ("skip".into(), self.skip.to_string()),
        ];
        q
    }
}

/// Query parameters for [`CalendarsService::list_user_availability_schedule`].
#[derive(Debug, Clone, Default)]
pub struct ListUserAvailabilityScheduleParams {
    /// Location ID to filter schedules by
    /// Required by the API.
    pub location_id: String,
    /// User ID to filter schedules by specific user
    /// Required by the API.
    pub user_id: String,
    /// Calendar ID for filtering schedules by specific calendar
    pub calendar_id: Option<String>,
    /// Number of items to skip for pagination
    pub skip: Option<f64>,
    /// Maximum number of items to return (max 500)
    pub limit: Option<f64>,
}

impl ListUserAvailabilityScheduleParams {
    /// Start from the parameters the API requires.
    pub fn new(location_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self {
            location_id: location_id.into(),
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Calendar ID for filtering schedules by specific calendar
    pub fn calendar_id(mut self, v: impl Into<String>) -> Self {
        self.calendar_id = Some(v.into());
        self
    }

    /// Number of items to skip for pagination
    pub fn skip(mut self, v: f64) -> Self {
        self.skip = Some(v);
        self
    }

    /// Maximum number of items to return (max 500)
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("locationId".into(), self.location_id.clone()),
            ("userId".into(), self.user_id.clone()),
        ];
        if let Some(v) = &self.calendar_id {
            q.push(("calendarId".into(), v.to_string()));
        }
        if let Some(v) = &self.skip {
            q.push(("skip".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`CalendarsService::get_free_slots`].
#[derive(Debug, Clone, Default)]
pub struct GetFreeSlotsParams {
    /// Start Date (**⚠️ Important:** Date range cannot be more than 31 days)
    /// Required by the API.
    pub start_date: f64,
    /// End Date (**⚠️ Important:** Date range cannot be more than 31 days)
    /// Required by the API.
    pub end_date: f64,
    /// The timezone in which the free slots are returned
    pub timezone: Option<String>,
    /// The user for whom the free slots are returned
    pub user_id: Option<String>,
    /// The users for whom the free slots are returned
    pub user_ids: Option<String>,
}

impl GetFreeSlotsParams {
    /// Start from the parameters the API requires.
    pub fn new(start_date: f64, end_date: f64) -> Self {
        Self {
            start_date,
            end_date,
            ..Default::default()
        }
    }

    /// The timezone in which the free slots are returned
    pub fn timezone(mut self, v: impl Into<String>) -> Self {
        self.timezone = Some(v.into());
        self
    }

    /// The user for whom the free slots are returned
    pub fn user_id(mut self, v: impl Into<String>) -> Self {
        self.user_id = Some(v.into());
        self
    }

    /// The users for whom the free slots are returned
    pub fn user_ids(mut self, v: impl Into<String>) -> Self {
        self.user_ids = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("startDate".into(), self.start_date.to_string()),
            ("endDate".into(), self.end_date.to_string()),
        ];
        if let Some(v) = &self.timezone {
            q.push(("timezone".into(), v.to_string()));
        }
        if let Some(v) = &self.user_id {
            q.push(("userId".into(), v.to_string()));
        }
        if let Some(v) = &self.user_ids {
            q.push(("userIds".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`CalendarsService::get_notifications`].
#[derive(Debug, Clone, Default)]
pub struct GetNotificationsParams {
    /// `isActive` query parameter.
    pub is_active: Option<bool>,
    /// `deleted` query parameter.
    pub deleted: Option<bool>,
    /// Number of records to return
    pub limit: Option<f64>,
    /// Number of records to skip
    pub skip: Option<f64>,
}

impl GetNotificationsParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Set the `isActive` query parameter.
    pub fn is_active(mut self, v: bool) -> Self {
        self.is_active = Some(v);
        self
    }

    /// Set the `deleted` query parameter.
    pub fn deleted(mut self, v: bool) -> Self {
        self.deleted = Some(v);
        self
    }

    /// Number of records to return
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// Number of records to skip
    pub fn skip(mut self, v: f64) -> Self {
        self.skip = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.is_active {
            q.push(("isActive".into(), v.to_string()));
        }
        if let Some(v) = &self.deleted {
            q.push(("deleted".into(), v.to_string()));
        }
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.skip {
            q.push(("skip".into(), v.to_string()));
        }
        q
    }
}

impl CalendarsService {
    /// Get Calendars
    ///
    /// Get all calendars in a location.
    ///
    /// `GET /calendars/`
    ///
    /// Requires scope: `calendars.readonly`.
    pub async fn get_calendars(
        &self,
        params: &GetCalendarsParams,
    ) -> Result<models::CalendarsGetSuccessfulResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/calendars/",
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Create Calendar
    ///
    /// Create calendar in a location.
    ///
    /// `POST /calendars/`
    ///
    /// Requires scope: `calendars.write`.
    pub async fn create_calendar(
        &self,
        body: &models::CalendarCreateDTO,
    ) -> Result<models::CalendarByIdSuccessfulResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/calendars/",
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Notes
    ///
    /// Get Appointment Notes
    ///
    /// `GET /calendars/appointments/{appointmentId}/notes`
    ///
    /// Requires scope: `calendars/events.readonly`.
    pub async fn get_notes(
        &self,
        appointment_id: &str,
        params: &GetNotesParams,
    ) -> Result<models::GetNotesListSuccessfulResponseDto> {
        let path = format!(
            "/calendars/appointments/{}/notes",
            crate::services::encode(appointment_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Create Note
    ///
    /// `POST /calendars/appointments/{appointmentId}/notes`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn create_note(
        &self,
        appointment_id: &str,
        body: &models::NotesDTO,
    ) -> Result<models::GetCreateUpdateNoteSuccessfulResponseDto> {
        let path = format!(
            "/calendars/appointments/{}/notes",
            crate::services::encode(appointment_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Delete Note
    ///
    /// `DELETE /calendars/appointments/{appointmentId}/notes/{noteId}`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn delete_note(
        &self,
        appointment_id: &str,
        note_id: &str,
    ) -> Result<models::DeleteNoteSuccessfulResponseDto> {
        let path = format!(
            "/calendars/appointments/{}/notes/{}",
            crate::services::encode(appointment_id),
            crate::services::encode(note_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Update Note
    ///
    /// `PUT /calendars/appointments/{appointmentId}/notes/{noteId}`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn update_note(
        &self,
        appointment_id: &str,
        note_id: &str,
        body: &models::NotesDTO,
    ) -> Result<models::GetCreateUpdateNoteSuccessfulResponseDto> {
        let path = format!(
            "/calendars/appointments/{}/notes/{}",
            crate::services::encode(appointment_id),
            crate::services::encode(note_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Blocked Slots
    ///
    /// `GET /calendars/blocked-slots`
    ///
    /// Requires scope: `calendars/events.readonly`.
    pub async fn get_blocked_slots(
        &self,
        params: &GetBlockedSlotsParams,
    ) -> Result<models::GetCalendarEventsSuccessfulResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/calendars/blocked-slots",
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Calendar Events
    ///
    /// `GET /calendars/events`
    ///
    /// Requires scope: `calendars/events.readonly`.
    pub async fn get_calendar_events(
        &self,
        params: &GetCalendarEventsParams,
    ) -> Result<models::GetCalendarEventsSuccessfulResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/calendars/events",
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Create appointment
    ///
    /// `POST /calendars/events/appointments`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn create_appointment_op(
        &self,
        body: &models::AppointmentCreateSchema,
    ) -> Result<models::AppointmentSchemaResponse> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/calendars/events/appointments",
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Appointment
    ///
    /// Get appointment by ID
    ///
    /// `GET /calendars/events/appointments/{eventId}`
    ///
    /// Requires scope: `calendars/events.readonly`.
    pub async fn get_appointment_op(
        &self,
        event_id: &str,
    ) -> Result<models::GetCalendarEventSuccessfulResponseDTO> {
        let path = format!(
            "/calendars/events/appointments/{}",
            crate::services::encode(event_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Update Appointment
    ///
    /// Update appointment
    ///
    /// `PUT /calendars/events/appointments/{eventId}`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn update_appointment(
        &self,
        event_id: &str,
        body: &models::AppointmentEditSchema,
    ) -> Result<models::AppointmentSchemaResponse> {
        let path = format!(
            "/calendars/events/appointments/{}",
            crate::services::encode(event_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Create Block Slot
    ///
    /// Create block slot
    ///
    /// `POST /calendars/events/block-slots`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn create_block_slot(
        &self,
        body: &models::BlockSlotCreateRequestDTO,
    ) -> Result<models::BlockedSlotSuccessfulResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/calendars/events/block-slots",
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Update Block Slot
    ///
    /// Update block slot by ID
    ///
    /// `PUT /calendars/events/block-slots/{eventId}`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn update_block_slot(
        &self,
        event_id: &str,
        body: &models::BlockSlotEditRequestDTO,
    ) -> Result<models::BlockedSlotSuccessfulResponseDto> {
        let path = format!(
            "/calendars/events/block-slots/{}",
            crate::services::encode(event_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Delete Event
    ///
    /// Delete event by ID
    ///
    /// `DELETE /calendars/events/{eventId}`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn delete_event(
        &self,
        event_id: &str,
        body: &models::DeleteAppointmentSchema,
    ) -> Result<models::DeleteEventSuccessfulResponseDto> {
        let path = format!("/calendars/events/{}", crate::services::encode(event_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Groups
    ///
    /// Get all calendar groups in a location.
    ///
    /// `GET /calendars/groups`
    ///
    /// Requires scope: `calendars/groups.readonly`.
    pub async fn get_groups(
        &self,
        params: &GetGroupsParams,
    ) -> Result<models::AllGroupsSuccessfulResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/calendars/groups",
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Create Calendar Group
    ///
    /// `POST /calendars/groups`
    ///
    /// Requires scope: `calendars/groups.write`.
    pub async fn create_calendar_group(
        &self,
        body: &models::GroupCreateDTO,
    ) -> Result<models::GroupCreateSuccessfulResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/calendars/groups",
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Validate group slug
    ///
    /// Validate if group slug is available or not.
    ///
    /// `POST /calendars/groups/validate-slug`
    ///
    /// Requires scope: `calendars/groups.write`.
    pub async fn validate_group_slug(
        &self,
        body: &models::ValidateGroupSlugPostBody,
    ) -> Result<models::ValidateGroupSlugSuccessResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/calendars/groups/validate-slug",
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Delete Group
    ///
    /// `DELETE /calendars/groups/{groupId}`
    ///
    /// Requires scope: `calendars/groups.write`.
    pub async fn delete_group(&self, group_id: &str) -> Result<models::GroupSuccessfulResponseDTO> {
        let path = format!("/calendars/groups/{}", crate::services::encode(group_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Update Group
    ///
    /// Update Group by group ID
    ///
    /// `PUT /calendars/groups/{groupId}`
    ///
    /// Requires scope: `calendars/groups.write`.
    pub async fn update_group(
        &self,
        group_id: &str,
        body: &models::GroupUpdateDTO,
    ) -> Result<models::GroupCreateSuccessfulResponseDTO> {
        let path = format!("/calendars/groups/{}", crate::services::encode(group_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Disable Group
    ///
    /// `PUT /calendars/groups/{groupId}/status`
    ///
    /// Requires scope: `calendars/groups.write`.
    pub async fn disable_group(
        &self,
        group_id: &str,
        body: &models::GroupStatusUpdateParams,
    ) -> Result<models::GroupSuccessfulResponseDTO> {
        let path = format!(
            "/calendars/groups/{}/status",
            crate::services::encode(group_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// List Calendar Resources
    ///
    /// List calendar resources by resource type and location ID
    ///
    /// `GET /calendars/resources/{resourceType}`
    ///
    /// Requires scope: `calendars/resources.readonly`.
    pub async fn list_calendar_resources(
        &self,
        resource_type: &str,
        params: &ListCalendarResourcesParams,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/calendars/resources/{}",
            crate::services::encode(resource_type)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Create Calendar Resource
    ///
    /// Create calendar resource by resource type
    ///
    /// `POST /calendars/resources/{resourceType}`
    ///
    /// Requires scope: `calendars/resources.write`.
    pub async fn create_calendar_resource(
        &self,
        resource_type: &str,
        body: &models::CreateCalendarResourceDTO,
    ) -> Result<models::CalendarResourceByIdResponseDTO> {
        let path = format!(
            "/calendars/resources/{}",
            crate::services::encode(resource_type)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Delete Calendar Resource
    ///
    /// Delete calendar resource by ID
    ///
    /// `DELETE /calendars/resources/{resourceType}/{id}`
    ///
    /// Requires scope: `calendars/resources.write`.
    pub async fn delete_calendar_resource(
        &self,
        resource_type: &str,
        id: &str,
    ) -> Result<models::ResourceDeleteResponseDTO> {
        let path = format!(
            "/calendars/resources/{}/{}",
            crate::services::encode(resource_type),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Calendar Resource
    ///
    /// Get calendar resource by ID
    ///
    /// `GET /calendars/resources/{resourceType}/{id}`
    ///
    /// Requires scope: `calendars/resources.readonly`.
    pub async fn get_calendar_resource(
        &self,
        resource_type: &str,
        id: &str,
    ) -> Result<models::CalendarResourceByIdResponseDTO> {
        let path = format!(
            "/calendars/resources/{}/{}",
            crate::services::encode(resource_type),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Update Calendar Resource
    ///
    /// Update calendar resource by ID
    ///
    /// `PUT /calendars/resources/{resourceType}/{id}`
    ///
    /// Requires scope: `calendars/resources.write`.
    pub async fn update_calendar_resource(
        &self,
        resource_type: &str,
        id: &str,
        body: &models::UpdateCalendarResourceDTO,
    ) -> Result<models::CalendarResourceResponseDTO> {
        let path = format!(
            "/calendars/resources/{}/{}",
            crate::services::encode(resource_type),
            crate::services::encode(id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Create user availability schedule
    ///
    /// Create new schedule with specified rules, timezone, location, user and calendar
    /// associations.
    ///
    /// `POST /calendars/schedules`
    ///
    /// Requires scope: `calendars.write`.
    pub async fn create_user_availability_schedule(
        &self,
        body: &models::CreateScheduleDTO,
    ) -> Result<models::ScheduleResponseDTO> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/calendars/schedules",
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// List user availability schedule
    ///
    /// Retrieve user availability schedules based on various filters including location,
    /// calendar, and user. Supports pagination.
    ///
    /// `GET /calendars/schedules/search`
    ///
    /// Requires scope: `calendars.readonly`.
    pub async fn list_user_availability_schedule(
        &self,
        params: &ListUserAvailabilityScheduleParams,
    ) -> Result<models::GetAllSchedulesResponseDTO> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/calendars/schedules/search",
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Delete user availability schedule
    ///
    /// Permanently remove a schedule and all its associated rules. This action cannot be
    /// undone.
    ///
    /// `DELETE /calendars/schedules/{id}`
    ///
    /// Requires scope: `calendars.write`.
    pub async fn delete_user_availability_schedule(&self, id: &str) -> Result<serde_json::Value> {
        let path = format!("/calendars/schedules/{}", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get user availability schedule
    ///
    /// Retrieve a specific schedule by its unique identifier. Returns detailed information
    /// including rules, timezone, and associated calendars/users.
    ///
    /// `GET /calendars/schedules/{id}`
    ///
    /// Requires scope: `calendars.readonly`.
    pub async fn get_user_availability_schedule(
        &self,
        id: &str,
    ) -> Result<models::ScheduleResponseDTO> {
        let path = format!("/calendars/schedules/{}", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Update user availability schedule
    ///
    /// Modify an existing schedule by updating its rules, timezone, and name All fields are
    /// optional - only provided fields will be updated.
    ///
    /// `PUT /calendars/schedules/{id}`
    ///
    /// Requires scope: `calendars.write`.
    pub async fn update_user_availability_schedule(
        &self,
        id: &str,
        body: &models::UpdateScheduleDTO,
    ) -> Result<models::ScheduleResponseDTO> {
        let path = format!("/calendars/schedules/{}", crate::services::encode(id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Remove user availability schedule from a calendar
    ///
    /// Removes the association between a team calendar and the given schedule by removing
    /// the calendarId from the schedule
    ///
    /// `DELETE /calendars/schedules/{id}/associations/{calendarId}`
    ///
    /// Requires scope: `calendars.write`.
    pub async fn remove_user_availability_schedule_from_a_calendar(
        &self,
        id: &str,
        calendar_id: &str,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/calendars/schedules/{}/associations/{}",
            crate::services::encode(id),
            crate::services::encode(calendar_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Apply user availability schedule to a calendar
    ///
    /// Associates a calendar with the given schedule by adding the calendarId to a schedule
    ///
    /// `PUT /calendars/schedules/{id}/associations/{calendarId}`
    ///
    /// Requires scope: `calendars.write`.
    pub async fn apply_user_availability_schedule_to_a_calendar(
        &self,
        id: &str,
        calendar_id: &str,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/calendars/schedules/{}/associations/{}",
            crate::services::encode(id),
            crate::services::encode(calendar_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Delete Calendar
    ///
    /// Delete calendar by ID
    ///
    /// `DELETE /calendars/{calendarId}`
    ///
    /// Requires scope: `calendars.write`.
    pub async fn delete_calendar(
        &self,
        calendar_id: &str,
    ) -> Result<models::CalendarDeleteSuccessfulResponseDTO> {
        let path = format!("/calendars/{}", crate::services::encode(calendar_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Calendar
    ///
    /// Get calendar by ID
    ///
    /// `GET /calendars/{calendarId}`
    ///
    /// Requires scope: `calendars.readonly`.
    pub async fn get_calendar(
        &self,
        calendar_id: &str,
    ) -> Result<models::CalendarByIdSuccessfulResponseDTO> {
        let path = format!("/calendars/{}", crate::services::encode(calendar_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Update Calendar
    ///
    /// Update calendar by ID.
    ///
    /// `PUT /calendars/{calendarId}`
    ///
    /// Requires scope: `calendars.write`.
    pub async fn update_calendar(
        &self,
        calendar_id: &str,
        body: &models::CalendarUpdateDTO,
    ) -> Result<models::CalendarByIdSuccessfulResponseDTO> {
        let path = format!("/calendars/{}", crate::services::encode(calendar_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Free Slots
    ///
    /// Get free slots for a calendar between a date range. Optionally a consumer can also
    /// request free slots in a particular timezone and also for a particular user.
    ///
    /// `GET /calendars/{calendarId}/free-slots`
    ///
    /// Requires scope: `calendars.readonly`.
    pub async fn get_free_slots(
        &self,
        calendar_id: &str,
        params: &GetFreeSlotsParams,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/calendars/{}/free-slots",
            crate::services::encode(calendar_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get notifications
    ///
    /// Get calendar notifications based on query
    ///
    /// `GET /calendars/{calendarId}/notifications`
    ///
    /// Requires scope: `calendars/events.readonly`.
    pub async fn get_notifications(
        &self,
        calendar_id: &str,
        params: &GetNotificationsParams,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/calendars/{}/notifications",
            crate::services::encode(calendar_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Create notification
    ///
    /// Create Calendar notifications, either one or multiple. All notification settings
    /// must be for single calendar only
    ///
    /// `POST /calendars/{calendarId}/notifications`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn create_notification(
        &self,
        calendar_id: &str,
        body: &serde_json::Value,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/calendars/{}/notifications",
            crate::services::encode(calendar_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Delete Notification
    ///
    /// Delete notification
    ///
    /// `DELETE /calendars/{calendarId}/notifications/{notificationId}`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn delete_notification(
        &self,
        calendar_id: &str,
        notification_id: &str,
    ) -> Result<models::CalendarNotificationDeleteResponseDTO> {
        let path = format!(
            "/calendars/{}/notifications/{}",
            crate::services::encode(calendar_id),
            crate::services::encode(notification_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::DELETE,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get notification
    ///
    /// Find Event notification by notificationId
    ///
    /// `GET /calendars/{calendarId}/notifications/{notificationId}`
    ///
    /// Requires scope: `calendars/events.readonly`.
    pub async fn get_notification(
        &self,
        calendar_id: &str,
        notification_id: &str,
    ) -> Result<models::CalendarNotificationResponseDTO> {
        let path = format!(
            "/calendars/{}/notifications/{}",
            crate::services::encode(calendar_id),
            crate::services::encode(notification_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Update notification
    ///
    /// Update Event notification by id
    ///
    /// `PUT /calendars/{calendarId}/notifications/{notificationId}`
    ///
    /// Requires scope: `calendars/events.write`.
    pub async fn update_notification(
        &self,
        calendar_id: &str,
        notification_id: &str,
        body: &models::UpdateCalendarNotificationsDTO,
    ) -> Result<models::CalendarNotificationDeleteResponseDTO> {
        let path = format!(
            "/calendars/{}/notifications/{}",
            crate::services::encode(calendar_id),
            crate::services::encode(notification_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }
}
