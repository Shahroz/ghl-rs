// @generated by xtask/generate_services.py — do not edit by hand.
//! `saas-api` — typed methods for all 22 API v2 operations
//! in this module.
//!
//! Access via [`Ghl::saas_api`](crate::Ghl::saas_api).
//!
//! Request and response types come from [`ghl_models::v2::saas_api`](https://docs.rs/ghl-models/latest/ghl_models/v2/saas_api/); every endpoint is also documented in the
//! [`saas-api` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/saas-api.md).
//!
//! Enable with `features = ["saas-api"]`.

#![allow(clippy::too_many_arguments)]

use crate::client::Ghl;
use crate::error::Result;
use ghl_models::v2::saas_api as models;

/// Typed access to the `saas-api` API v2 surface (22 operations). Obtained via
/// [`Ghl::saas_api`](crate::Ghl::saas_api).
#[derive(Debug, Clone)]
pub struct SaasApiService {
    pub(crate) client: Ghl,
}

impl SaasApiService {
    pub(crate) fn new(client: Ghl) -> Self {
        Self { client }
    }
}

/// Query parameters for [`SaasApiService::get_location_subscription_details`].
#[derive(Debug, Clone, Default)]
pub struct GetLocationSubscriptionDetailsParams {
    /// Company ID to filter subscription details
    /// Required by the API.
    pub company_id: String,
}

impl GetLocationSubscriptionDetailsParams {
    /// Start from the parameters the API requires.
    pub fn new(company_id: impl Into<String>) -> Self {
        Self {
            company_id: company_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("companyId".into(), self.company_id.clone())];
        q
    }
}

/// Query parameters for [`SaasApiService::get_locations_by_stripe_id_with_company_id`].
#[derive(Debug, Clone, Default)]
pub struct GetLocationsByStripeIdWithCompanyIdParams {
    /// Stripe customer ID to find locations for
    pub customer_id: Option<String>,
    /// Stripe subscription ID to find locations for
    pub subscription_id: Option<String>,
    /// Company ID to filter locations
    /// Required by the API.
    pub company_id: String,
}

impl GetLocationsByStripeIdWithCompanyIdParams {
    /// Start from the parameters the API requires.
    pub fn new(company_id: impl Into<String>) -> Self {
        Self {
            company_id: company_id.into(),
            ..Default::default()
        }
    }

    /// Stripe customer ID to find locations for
    pub fn customer_id(mut self, v: impl Into<String>) -> Self {
        self.customer_id = Some(v.into());
        self
    }

    /// Stripe subscription ID to find locations for
    pub fn subscription_id(mut self, v: impl Into<String>) -> Self {
        self.subscription_id = Some(v.into());
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![("companyId".into(), self.company_id.clone())];
        if let Some(v) = &self.customer_id {
            q.push(("customerId".into(), v.to_string()));
        }
        if let Some(v) = &self.subscription_id {
            q.push(("subscriptionId".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`SaasApiService::get_saa_s_locations`].
#[derive(Debug, Clone, Default)]
pub struct GetSaaSLocationsParams {
    /// Page number for pagination
    pub page: Option<f64>,
}

impl GetSaaSLocationsParams {
    /// Start from the parameters the API requires.
    pub fn new() -> Self {
        Self {
            ..Default::default()
        }
    }

    /// Page number for pagination
    pub fn page(mut self, v: f64) -> Self {
        self.page = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.page {
            q.push(("page".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`SaasApiService::get_saa_s_plan`].
#[derive(Debug, Clone, Default)]
pub struct GetSaaSPlanParams {
    /// Company ID to filter SaaS plan
    /// Required by the API.
    pub company_id: String,
}

impl GetSaaSPlanParams {
    /// Start from the parameters the API requires.
    pub fn new(company_id: impl Into<String>) -> Self {
        Self {
            company_id: company_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("companyId".into(), self.company_id.clone())];
        q
    }
}

/// Query parameters for [`SaasApiService::get_location_subscription_details_op`].
#[derive(Debug, Clone, Default)]
pub struct GetLocationSubscriptionDetailsOpParams {
    /// `companyId` query parameter.
    /// Required by the API.
    pub company_id: String,
}

impl GetLocationSubscriptionDetailsOpParams {
    /// Start from the parameters the API requires.
    pub fn new(company_id: impl Into<String>) -> Self {
        Self {
            company_id: company_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("companyId".into(), self.company_id.clone())];
        q
    }
}

/// Query parameters for
/// [`SaasApiService::get_locations_by_stripe_id_with_company_id_op`].
#[derive(Debug, Clone, Default)]
pub struct GetLocationsByStripeIdWithCompanyIdOpParams {
    /// `customerId` query parameter.
    /// Required by the API.
    pub customer_id: String,
    /// `subscriptionId` query parameter.
    /// Required by the API.
    pub subscription_id: String,
    /// `companyId` query parameter.
    /// Required by the API.
    pub company_id: String,
}

impl GetLocationsByStripeIdWithCompanyIdOpParams {
    /// Start from the parameters the API requires.
    pub fn new(
        customer_id: impl Into<String>,
        subscription_id: impl Into<String>,
        company_id: impl Into<String>,
    ) -> Self {
        Self {
            customer_id: customer_id.into(),
            subscription_id: subscription_id.into(),
            company_id: company_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("customerId".into(), self.customer_id.clone()),
            ("subscriptionId".into(), self.subscription_id.clone()),
            ("companyId".into(), self.company_id.clone()),
        ];
        q
    }
}

/// Query parameters for [`SaasApiService::get_saa_s_locations_op`].
#[derive(Debug, Clone, Default)]
pub struct GetSaaSLocationsOpParams {
    /// `page` query parameter.
    /// Required by the API.
    pub page: f64,
}

impl GetSaaSLocationsOpParams {
    /// Start from the parameters the API requires.
    pub fn new(page: f64) -> Self {
        Self { page }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("page".into(), self.page.to_string())];
        q
    }
}

/// Query parameters for [`SaasApiService::get_saa_s_plan_op`].
#[derive(Debug, Clone, Default)]
pub struct GetSaaSPlanOpParams {
    /// `companyId` query parameter.
    /// Required by the API.
    pub company_id: String,
}

impl GetSaaSPlanOpParams {
    /// Start from the parameters the API requires.
    pub fn new(company_id: impl Into<String>) -> Self {
        Self {
            company_id: company_id.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![("companyId".into(), self.company_id.clone())];
        q
    }
}

impl SaasApiService {
    /// Get Agency Plans
    ///
    /// Fetch all agency subscription plans for a given company ID
    ///
    /// `GET /saas-api/public-api/agency-plans/{companyId}`
    pub async fn get_agency_plans(&self, company_id: &str) -> Result<serde_json::Value> {
        let path = format!(
            "/saas-api/public-api/agency-plans/{}",
            crate::services::encode(company_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Disable SaaS for locations
    ///
    /// Disable SaaS for locations for given locationIds
    ///
    /// `POST /saas-api/public-api/bulk-disable-saas/{companyId}`
    pub async fn disable_saa_s_for_locations(
        &self,
        company_id: &str,
        body: &models::BulkDisableSaasDto,
    ) -> Result<models::BulkDisableSaasResponseDto> {
        let path = format!(
            "/saas-api/public-api/bulk-disable-saas/{}",
            crate::services::encode(company_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Bulk Enable SaaS
    ///
    /// Enable SaaS mode for multiple locations with support for both SaaS v1 and v2
    ///
    /// `POST /saas-api/public-api/bulk-enable-saas/{companyId}`
    pub async fn bulk_enable_saa_s(
        &self,
        company_id: &str,
        body: &models::BulkEnableSaasRequestDto,
    ) -> Result<models::BulkEnableSaasResponseDto> {
        let path = format!(
            "/saas-api/public-api/bulk-enable-saas/{}",
            crate::services::encode(company_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Enable SaaS for Sub-Account (Formerly Location)
    ///
    /// Enable SaaS for Sub-Account (Formerly Location) based on the data provided ! This
    /// feature is only available on Agency Pro ($497) plan.
    ///
    /// `POST /saas-api/public-api/enable-saas/{locationId}`
    pub async fn enable_saa_s_for_sub_account_formerly_location(
        &self,
        location_id: &str,
        body: &models::EnableSaasDto,
    ) -> Result<models::EnableSaasResponseDto> {
        let path = format!(
            "/saas-api/public-api/enable-saas/{}",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Location Subscription Details
    ///
    /// Fetch subscription details for a specific location from location metadata
    ///
    /// `GET /saas-api/public-api/get-saas-subscription/{locationId}`
    pub async fn get_location_subscription_details(
        &self,
        location_id: &str,
        params: &GetLocationSubscriptionDetailsParams,
    ) -> Result<models::LocationSubscriptionResponseDto> {
        let path = format!(
            "/saas-api/public-api/get-saas-subscription/{}",
            crate::services::encode(location_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get locations by stripeId with companyId
    ///
    /// Get locations by stripeCustomerId or stripeSubscriptionId with companyId
    ///
    /// `GET /saas-api/public-api/locations`
    pub async fn get_locations_by_stripe_id_with_company_id(
        &self,
        params: &GetLocationsByStripeIdWithCompanyIdParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/saas-api/public-api/locations",
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Pause location
    ///
    /// Pause Sub account for given locationId
    ///
    /// `POST /saas-api/public-api/pause/{locationId}`
    pub async fn pause_location(
        &self,
        location_id: &str,
        body: &models::PauseLocationDto,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/saas-api/public-api/pause/{}",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get SaaS Locations
    ///
    /// Fetch all SaaS-activated locations for a company with pagination
    ///
    /// `GET /saas-api/public-api/saas-locations/{companyId}`
    pub async fn get_saa_s_locations(
        &self,
        company_id: &str,
        params: &GetSaaSLocationsParams,
    ) -> Result<models::GetSaasLocationsResponseDto> {
        let path = format!(
            "/saas-api/public-api/saas-locations/{}",
            crate::services::encode(company_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get SaaS Plan
    ///
    /// Fetch a specific SaaS plan by plan ID
    ///
    /// `GET /saas-api/public-api/saas-plan/{planId}`
    pub async fn get_saa_s_plan(
        &self,
        plan_id: &str,
        params: &GetSaaSPlanParams,
    ) -> Result<models::SaasPlanResponseDto> {
        let path = format!(
            "/saas-api/public-api/saas-plan/{}",
            crate::services::encode(plan_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Update Rebilling
    ///
    /// Bulk update rebilling for given locationIds
    ///
    /// `POST /saas-api/public-api/update-rebilling/{companyId}`
    pub async fn update_rebilling(
        &self,
        company_id: &str,
        body: &models::UpdateRebillingDto,
    ) -> Result<models::UpdateRebillingResponseDto> {
        let path = format!(
            "/saas-api/public-api/update-rebilling/{}",
            crate::services::encode(company_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Update SaaS subscription
    ///
    /// Update SaaS subscription for given locationId and customerId
    ///
    /// `PUT /saas-api/public-api/update-saas-subscription/{locationId}`
    pub async fn update_saa_s_subscription(
        &self,
        location_id: &str,
        body: &models::UpdateSubscriptionDto,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/saas-api/public-api/update-saas-subscription/{}",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Agency Plans
    ///
    /// Fetch all agency subscription plans for a given company ID
    ///
    /// `GET /saas/agency-plans/{companyId}`
    pub async fn get_agency_plans_op(&self, company_id: &str) -> Result<serde_json::Value> {
        let path = format!("/saas/agency-plans/{}", crate::services::encode(company_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Disable SaaS for locations
    ///
    /// Disable SaaS for locations for given locationIds
    ///
    /// `POST /saas/bulk-disable-saas/{companyId}`
    pub async fn disable_saa_s_for_locations_op(
        &self,
        company_id: &str,
        body: &models::BulkDisableSaasDto,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/saas/bulk-disable-saas/{}",
            crate::services::encode(company_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Bulk Enable SaaS
    ///
    /// Enable SaaS mode for multiple locations with support for both SaaS v1 and v2
    ///
    /// `POST /saas/bulk-enable-saas/{companyId}`
    pub async fn bulk_enable_saa_s_op(
        &self,
        company_id: &str,
        body: &models::BulkEnableSaasRequestDto,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/saas/bulk-enable-saas/{}",
            crate::services::encode(company_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Enable SaaS for Sub-Account (Formerly Location)
    ///
    /// Enable SaaS for Sub-Account (Formerly Location) based on the data provided ! This
    /// feature is only available on Agency Pro ($497) plan.
    ///
    /// `POST /saas/enable-saas/{locationId}`
    pub async fn enable_saa_s_for_sub_account_formerly_location_op(
        &self,
        location_id: &str,
        body: &models::EnableSaasDto,
    ) -> Result<serde_json::Value> {
        let path = format!("/saas/enable-saas/{}", crate::services::encode(location_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get Location Subscription Details
    ///
    /// Fetch subscription details for a specific location from location metadata
    ///
    /// `GET /saas/get-saas-subscription/{locationId}`
    pub async fn get_location_subscription_details_op(
        &self,
        location_id: &str,
        params: &GetLocationSubscriptionDetailsOpParams,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/saas/get-saas-subscription/{}",
            crate::services::encode(location_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get locations by stripeId with companyId
    ///
    /// Get locations by stripeCustomerId or stripeSubscriptionId with companyId
    ///
    /// `GET /saas/locations`
    pub async fn get_locations_by_stripe_id_with_company_id_op(
        &self,
        params: &GetLocationsByStripeIdWithCompanyIdOpParams,
    ) -> Result<serde_json::Value> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/saas/locations",
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Pause location
    ///
    /// Pause Sub account for given locationId
    ///
    /// `POST /saas/pause/{locationId}`
    pub async fn pause_location_op(
        &self,
        location_id: &str,
        body: &models::PauseLocationDto,
    ) -> Result<serde_json::Value> {
        let path = format!("/saas/pause/{}", crate::services::encode(location_id));
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Get SaaS Locations
    ///
    /// Fetch all SaaS-activated locations for a company with pagination
    ///
    /// `GET /saas/saas-locations/{companyId}`
    pub async fn get_saa_s_locations_op(
        &self,
        company_id: &str,
        params: &GetSaaSLocationsOpParams,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/saas/saas-locations/{}",
            crate::services::encode(company_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Get SaaS Plan
    ///
    /// Fetch a specific SaaS plan by plan ID
    ///
    /// `GET /saas/saas-plan/{planId}`
    pub async fn get_saa_s_plan_op(
        &self,
        plan_id: &str,
        params: &GetSaaSPlanOpParams,
    ) -> Result<serde_json::Value> {
        let path = format!("/saas/saas-plan/{}", crate::services::encode(plan_id));
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                &path,
                &query,
                None::<&()>,
                Some("2021-04-15"),
            )
            .await
    }

    /// Update Rebilling
    ///
    /// Bulk update rebilling for given locationIds
    ///
    /// `POST /saas/update-rebilling/{companyId}`
    pub async fn update_rebilling_op(
        &self,
        company_id: &str,
        body: &models::UpdateRebillingDto,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/saas/update-rebilling/{}",
            crate::services::encode(company_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }

    /// Update SaaS subscription
    ///
    /// Update SaaS subscription for given locationId and customerId
    ///
    /// `PUT /saas/update-saas-subscription/{locationId}`
    pub async fn update_saa_s_subscription_op(
        &self,
        location_id: &str,
        body: &models::UpdateSubscriptionDto,
    ) -> Result<serde_json::Value> {
        let path = format!(
            "/saas/update-saas-subscription/{}",
            crate::services::encode(location_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::PUT,
                &path,
                &query,
                Some(body),
                Some("2021-04-15"),
            )
            .await
    }
}
