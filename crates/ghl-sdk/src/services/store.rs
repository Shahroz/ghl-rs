// @generated by xtask/generate_services.py — do not edit by hand.
//! `store` — typed methods for all 18 API v2 operations
//! in this module.
//!
//! Access via [`Ghl::store`](crate::Ghl::store).
//!
//! Request and response types come from [`ghl_models::v2::store`](https://docs.rs/ghl-models/latest/ghl_models/v2/store/); every endpoint is also documented in the
//! [`store` API reference](https://github.com/Shahroz/ghl-rs/blob/main/docs/api/store.md).
//!
//! Enable with `features = ["store"]`.

#![allow(clippy::too_many_arguments)]

use crate::client::Ghl;
use crate::error::Result;
use ghl_models::v2::store as models;

/// Typed access to the `store` API v2 surface (18 operations). Obtained via
/// [`Ghl::store`](crate::Ghl::store).
#[derive(Debug, Clone)]
pub struct StoreService {
    pub(crate) client: Ghl,
}

impl StoreService {
    pub(crate) fn new(client: Ghl) -> Self {
        Self { client }
    }
}

/// Query parameters for [`StoreService::list_shipping_carriers`].
#[derive(Debug, Clone, Default)]
pub struct ListShippingCarriersParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl ListShippingCarriersParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`StoreService::delete_shipping_carrier`].
#[derive(Debug, Clone, Default)]
pub struct DeleteShippingCarrierParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl DeleteShippingCarrierParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`StoreService::get_shipping_carrier`].
#[derive(Debug, Clone, Default)]
pub struct GetShippingCarrierParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GetShippingCarrierParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`StoreService::list_shipping_zones`].
#[derive(Debug, Clone, Default)]
pub struct ListShippingZonesParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// The maximum number of items to be included in a single page of results
    pub limit: Option<f64>,
    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub offset: Option<f64>,
    /// Include shipping rates array
    pub with_shipping_rate: Option<bool>,
}

impl ListShippingZonesParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            ..Default::default()
        }
    }

    /// The maximum number of items to be included in a single page of results
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    /// Include shipping rates array
    pub fn with_shipping_rate(mut self, v: bool) -> Self {
        self.with_shipping_rate = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        if let Some(v) = &self.with_shipping_rate {
            q.push(("withShippingRate".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`StoreService::delete_shipping_zone`].
#[derive(Debug, Clone, Default)]
pub struct DeleteShippingZoneParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl DeleteShippingZoneParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`StoreService::get_shipping_zone`].
#[derive(Debug, Clone, Default)]
pub struct GetShippingZoneParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// Include shipping rates array
    pub with_shipping_rate: Option<bool>,
}

impl GetShippingZoneParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            ..Default::default()
        }
    }

    /// Include shipping rates array
    pub fn with_shipping_rate(mut self, v: bool) -> Self {
        self.with_shipping_rate = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        if let Some(v) = &self.with_shipping_rate {
            q.push(("withShippingRate".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`StoreService::list_shipping_rates`].
#[derive(Debug, Clone, Default)]
pub struct ListShippingRatesParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
    /// The maximum number of items to be included in a single page of results
    pub limit: Option<f64>,
    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub offset: Option<f64>,
}

impl ListShippingRatesParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
            ..Default::default()
        }
    }

    /// The maximum number of items to be included in a single page of results
    pub fn limit(mut self, v: f64) -> Self {
        self.limit = Some(v);
        self
    }

    /// The starting index of the page, indicating the position from which the results
    /// should be retrieved.
    pub fn offset(mut self, v: f64) -> Self {
        self.offset = Some(v);
        self
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        if let Some(v) = &self.limit {
            q.push(("limit".into(), v.to_string()));
        }
        if let Some(v) = &self.offset {
            q.push(("offset".into(), v.to_string()));
        }
        q
    }
}

/// Query parameters for [`StoreService::delete_shipping_rate`].
#[derive(Debug, Clone, Default)]
pub struct DeleteShippingRateParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl DeleteShippingRateParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`StoreService::get_shipping_rate`].
#[derive(Debug, Clone, Default)]
pub struct GetShippingRateParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GetShippingRateParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

/// Query parameters for [`StoreService::get_store_settings`].
#[derive(Debug, Clone, Default)]
pub struct GetStoreSettingsParams {
    /// Location Id or Agency Id
    /// Required by the API.
    pub alt_id: String,
    /// `altType` query parameter.
    /// Allowed values: `location`.
    /// Required by the API.
    pub alt_type: String,
}

impl GetStoreSettingsParams {
    /// Start from the parameters the API requires.
    pub fn new(alt_id: impl Into<String>, alt_type: impl Into<String>) -> Self {
        Self {
            alt_id: alt_id.into(),
            alt_type: alt_type.into(),
        }
    }

    fn to_query(&self) -> Vec<(String, String)> {
        let q: Vec<(String, String)> = vec![
            ("altId".into(), self.alt_id.clone()),
            ("altType".into(), self.alt_type.clone()),
        ];
        q
    }
}

impl StoreService {
    /// List Shipping Carriers
    ///
    /// The "List Shipping Carrier" API allows to retrieve a list of shipping carrier.
    ///
    /// `GET /store/shipping-carrier`
    pub async fn list_shipping_carriers(
        &self,
        params: &ListShippingCarriersParams,
    ) -> Result<models::ListShippingCarrierResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/store/shipping-carrier",
                &query,
                None::<&()>,
                None,
            )
            .await
    }

    /// Create Shipping Carrier
    ///
    /// The "Create Shipping Carrier" API allows adding a new shipping carrier.
    ///
    /// `POST /store/shipping-carrier`
    pub async fn create_shipping_carrier(
        &self,
        body: &models::CreateShippingCarrierDto,
    ) -> Result<models::CreateShippingCarrierResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/store/shipping-carrier",
                &query,
                Some(body),
                None,
            )
            .await
    }

    /// Delete shipping carrier
    ///
    /// Delete specific shipping carrier with Id :shippingCarrierId
    ///
    /// `DELETE /store/shipping-carrier/{shippingCarrierId}`
    pub async fn delete_shipping_carrier(
        &self,
        shipping_carrier_id: &str,
        params: &DeleteShippingCarrierParams,
    ) -> Result<models::DeleteShippingCarrierResponseDto> {
        let path = format!(
            "/store/shipping-carrier/{}",
            crate::services::encode(shipping_carrier_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::DELETE, &path, &query, None::<&()>, None)
            .await
    }

    /// Get Shipping Carrier
    ///
    /// The "List Shipping Carrier" API allows to retrieve a paginated list of shipping
    /// carrier.
    ///
    /// `GET /store/shipping-carrier/{shippingCarrierId}`
    pub async fn get_shipping_carrier(
        &self,
        shipping_carrier_id: &str,
        params: &GetShippingCarrierParams,
    ) -> Result<models::GetShippingCarrierResponseDto> {
        let path = format!(
            "/store/shipping-carrier/{}",
            crate::services::encode(shipping_carrier_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, None)
            .await
    }

    /// Update Shipping Carrier
    ///
    /// The "update Shipping Carrier" API allows update a shipping carrier to the system.
    ///
    /// `PUT /store/shipping-carrier/{shippingCarrierId}`
    pub async fn update_shipping_carrier(
        &self,
        shipping_carrier_id: &str,
        body: &models::UpdateShippingCarrierDto,
    ) -> Result<models::UpdateShippingCarrierResponseDto> {
        let path = format!(
            "/store/shipping-carrier/{}",
            crate::services::encode(shipping_carrier_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), None)
            .await
    }

    /// List Shipping Zones
    ///
    /// The "List Shipping Zone" API allows to retrieve a list of shipping zone.
    ///
    /// `GET /store/shipping-zone`
    pub async fn list_shipping_zones(
        &self,
        params: &ListShippingZonesParams,
    ) -> Result<models::ListShippingZoneResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/store/shipping-zone",
                &query,
                None::<&()>,
                None,
            )
            .await
    }

    /// Create Shipping Zone
    ///
    /// The "Create Shipping Zone" API allows adding a new shipping zone.
    ///
    /// `POST /store/shipping-zone`
    pub async fn create_shipping_zone(
        &self,
        body: &models::CreateShippingZoneDto,
    ) -> Result<models::CreateShippingZoneResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/store/shipping-zone",
                &query,
                Some(body),
                None,
            )
            .await
    }

    /// Get available shipping rates
    ///
    /// This return available shipping rates for country based on order amount
    ///
    /// `POST /store/shipping-zone/shipping-rates`
    pub async fn get_available_shipping_rates(
        &self,
        body: &models::GetAvailableShippingRates,
    ) -> Result<models::GetAvailableShippingRatesResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/store/shipping-zone/shipping-rates",
                &query,
                Some(body),
                None,
            )
            .await
    }

    /// Delete shipping zone
    ///
    /// Delete specific shipping zone with Id :shippingZoneId
    ///
    /// `DELETE /store/shipping-zone/{shippingZoneId}`
    pub async fn delete_shipping_zone(
        &self,
        shipping_zone_id: &str,
        params: &DeleteShippingZoneParams,
    ) -> Result<models::DeleteShippingZoneResponseDto> {
        let path = format!(
            "/store/shipping-zone/{}",
            crate::services::encode(shipping_zone_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::DELETE, &path, &query, None::<&()>, None)
            .await
    }

    /// Get Shipping Zone
    ///
    /// The "List Shipping Zone" API allows to retrieve a paginated list of shipping zone.
    ///
    /// `GET /store/shipping-zone/{shippingZoneId}`
    pub async fn get_shipping_zone(
        &self,
        shipping_zone_id: &str,
        params: &GetShippingZoneParams,
    ) -> Result<models::GetShippingZoneResponseDto> {
        let path = format!(
            "/store/shipping-zone/{}",
            crate::services::encode(shipping_zone_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, None)
            .await
    }

    /// Update Shipping Zone
    ///
    /// The "update Shipping Zone" API allows update a shipping zone to the system.
    ///
    /// `PUT /store/shipping-zone/{shippingZoneId}`
    pub async fn update_shipping_zone(
        &self,
        shipping_zone_id: &str,
        body: &models::UpdateShippingZoneDto,
    ) -> Result<models::UpdateShippingZoneResponseDto> {
        let path = format!(
            "/store/shipping-zone/{}",
            crate::services::encode(shipping_zone_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), None)
            .await
    }

    /// List Shipping Rates
    ///
    /// The "List Shipping Rate" API allows to retrieve a list of shipping rate.
    ///
    /// `GET /store/shipping-zone/{shippingZoneId}/shipping-rate`
    pub async fn list_shipping_rates(
        &self,
        shipping_zone_id: &str,
        params: &ListShippingRatesParams,
    ) -> Result<models::ListShippingRateResponseDto> {
        let path = format!(
            "/store/shipping-zone/{}/shipping-rate",
            crate::services::encode(shipping_zone_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, None)
            .await
    }

    /// Create Shipping Rate
    ///
    /// The "Create Shipping Rate" API allows adding a new shipping rate.
    ///
    /// `POST /store/shipping-zone/{shippingZoneId}/shipping-rate`
    pub async fn create_shipping_rate(
        &self,
        shipping_zone_id: &str,
        body: &models::CreateShippingRateDto,
    ) -> Result<models::CreateShippingRateResponseDto> {
        let path = format!(
            "/store/shipping-zone/{}/shipping-rate",
            crate::services::encode(shipping_zone_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::POST, &path, &query, Some(body), None)
            .await
    }

    /// Delete shipping rate
    ///
    /// Delete specific shipping rate with Id :shippingRateId
    ///
    /// `DELETE /store/shipping-zone/{shippingZoneId}/shipping-rate/{shippingRateId}`
    pub async fn delete_shipping_rate(
        &self,
        shipping_zone_id: &str,
        shipping_rate_id: &str,
        params: &DeleteShippingRateParams,
    ) -> Result<models::DeleteShippingRateResponseDto> {
        let path = format!(
            "/store/shipping-zone/{}/shipping-rate/{}",
            crate::services::encode(shipping_zone_id),
            crate::services::encode(shipping_rate_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::DELETE, &path, &query, None::<&()>, None)
            .await
    }

    /// Get Shipping Rate
    ///
    /// The "List Shipping Rate" API allows to retrieve a paginated list of shipping rate.
    ///
    /// `GET /store/shipping-zone/{shippingZoneId}/shipping-rate/{shippingRateId}`
    pub async fn get_shipping_rate(
        &self,
        shipping_zone_id: &str,
        shipping_rate_id: &str,
        params: &GetShippingRateParams,
    ) -> Result<models::GetShippingRateResponseDto> {
        let path = format!(
            "/store/shipping-zone/{}/shipping-rate/{}",
            crate::services::encode(shipping_zone_id),
            crate::services::encode(shipping_rate_id)
        );
        let query = params.to_query();
        self.client
            .send_versioned(reqwest::Method::GET, &path, &query, None::<&()>, None)
            .await
    }

    /// Update Shipping Rate
    ///
    /// The "update Shipping Rate" API allows update a shipping rate to the system.
    ///
    /// `PUT /store/shipping-zone/{shippingZoneId}/shipping-rate/{shippingRateId}`
    pub async fn update_shipping_rate(
        &self,
        shipping_zone_id: &str,
        shipping_rate_id: &str,
        body: &models::UpdateShippingRateDto,
    ) -> Result<models::UpdateShippingRateResponseDto> {
        let path = format!(
            "/store/shipping-zone/{}/shipping-rate/{}",
            crate::services::encode(shipping_zone_id),
            crate::services::encode(shipping_rate_id)
        );
        let query = Vec::new();
        self.client
            .send_versioned(reqwest::Method::PUT, &path, &query, Some(body), None)
            .await
    }

    /// Get Store Settings
    ///
    /// Get store settings by altId and altType.
    ///
    /// `GET /store/store-setting`
    pub async fn get_store_settings(
        &self,
        params: &GetStoreSettingsParams,
    ) -> Result<models::GetStoreSettingResponseDto> {
        let query = params.to_query();
        self.client
            .send_versioned(
                reqwest::Method::GET,
                "/store/store-setting",
                &query,
                None::<&()>,
                None,
            )
            .await
    }

    /// Create/Update Store Settings
    ///
    /// Create or update store settings by altId and altType.
    ///
    /// `POST /store/store-setting`
    pub async fn create_update_store_settings(
        &self,
        body: &models::CreateStoreSettingDto,
    ) -> Result<models::CreateStoreSettingResponseDto> {
        let query = Vec::new();
        self.client
            .send_versioned(
                reqwest::Method::POST,
                "/store/store-setting",
                &query,
                Some(body),
                None,
            )
            .await
    }
}
